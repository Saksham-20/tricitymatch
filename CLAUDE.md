# CLAUDE.md

> **Every session:** read this first · update after · use skills (never do manually what a skill does).

> **QA:** reusable playbook is `docs/QA.md` (web + admin + RN iOS/Android simulators). Web mobile-view + admin professionalism passes DONE (2026-06-18/19). **RN apps simulator QA pass DONE (2026-06-20)** — slop removed + 7 functional bugs fixed (status bar, gorhom crash, re-onboarding, dead Chat, missing subscriptionPlan, profile-data unwrap, completion ring). Tracker `rn-qa-progress.md`; see Audit History.

**TricityMatch** — hyperlocal matrimonial (Chandigarh/Mohali/Panchkula). React 18 SPA + Express + PostgreSQL + Redis + Socket.io + RN mobile. Docker Compose + Nginx.

## Commands
```bash
npm run dev | dev:backend(:5001) | dev:frontend(:3000)   # Vite proxies /api + /socket.io
npm run test | lint | lint:fix | build
# backend/: npm run migrate | migrate:undo | db:reset | db:create | db:drop; node seeders/adminSeeder.js
npm run qa | qa:auth | qa:visual | qa:headed | qa:full | qa:ui          # e2e
# mobile/ ONLY: npx expo start --clear ; node_modules/.bin/tsc --noEmit -p tsconfig.json
```
Ports: backend 5001 · web 3000 · Metro 8081.

## Layout
`backend/` Express (CommonJS) · `frontend/` React+Vite · `mobile/` RN Expo SDK51 · `shared/` TS types (@shared) · `docs/` (PRD/arch/security/spec/tickets/API) · `e2e/` Playwright (9) · `nginx/` · `monitoring/` Prom+Grafana · `docker-compose.yml`

## Backend
Entry `server.js`. Key files:
- `config/env.js` — sole env source (never read process.env elsewhere); prod guard `process.exit(1)` on dev JWT_SECRET/COOKIE_SECRET/DB_PASSWORD/FRONTEND_URL + RAZORPAY_WEBHOOK_SECRET/SMS/SMTP + BG_CHECK_WEBHOOK_SECRET (when provider enabled)
- `config/database.js` Sequelize+PG · `routes/index.js` mounts `/api/v1`+`/api`; marketing mounted in server.js at `/api/marketing`
- `middlewares/`: security (helmet, CORS, 9 limiters, Redis lockout, sanitize) · auth (JWT cookie, adminAuth, requirePremium/VIP, socketAuth) · errorHandler (AppError, asyncHandler) · logger (JSON) · upload (Multer+Cloudinary; MIME+extension filter + Cloudinary `allowed_formats` content-validation + pinned `resource_type` per endpoint; **SEC-4 full: `validateUploadedFiles` magic-byte check on disk-stored uploads** (Cloudinary-streamed files stay decode-validated by `allowed_formats`) — wired on profile-photo + verification-doc routes; voice-intro + video-intro resource_type=`video`, MP4/MOV/WebM ≤25MB)
- `socket/socketHandler.js` — join-room (mutual+premium gated), send-message (anti-spoof), typing (mutual-match gated), edit, message-deleted (server-authoritative from REST), online-status, **call signaling** (call-incoming/accepted/declined/ended to `user_<id>` rooms). **Family-group sockets ENABLED + membership-gated** (join-group verifies GroupMember; group writes are REST-only & broadcast `group-message-received`/`group-message-edited`/`group-message-deleted` server-authoritatively — closes old IDOR; review-progress SOCK-1/MF-1/REF-51)
- `controllers/groupController.js` + `routes/groupRoutes.js` — **family-group chat backend** (`/groups`): create/list/get/delete group, add/remove/invite (by userId OR phone)/leave member, list/post/edit/delete messages. `requireMembership` is the authz boundary. Models Group/GroupMember/GroupMessage (migration 000039).
- `utils/kundli.js` — **Kundli match-report PDF** (pdfkit): Ashtakoot guna table + Manglik + numerology + summary; streamed by `GET /profile/:id/horoscope-match/pdf` (premium)
- `utils/`: cache (Redis+in-mem fallback; `get/set/del`,`getString/setString`,`getNumber/setNumber`) · queue (Bull: email/cleanup/push; weekly digest Mon 10AM, saved-search-alerts daily 9AM) · notifyUser `notify(userId,type,title,body,relatedId)` · razorpay (PLANS, createOrder, verifyPayment [timing-safe HMAC], createGenericOrder; throws on placeholder secret) · agoraToken (`DEV_STUB_TOKEN` if unset) · smsService (Fast2SMS/MSG91 **4-digit OTP**; MSG91 v5 `/api/v5/otp` send forces `family:4` [authkey IP-whitelist is IPv4-only — IPv6 egress → 418 AuthenticationFailure]; local OTP-gen+verify vs Redis, MSG91 only delivers; dev logs OTP only when SMS unconfigured; 3/hr) · bgCheckService (AuthBridge+Signzy, dev stub auto-pass 5s; webhook verify **fails CLOSED in prod** if secret unset) · email (primary; **Resend-first + SMTP-fallback provider layer**, `deliver()` channel-routed [transactional→Resend, documents→SMTP], `sendEmail` accepts 3 call shapes, templates incl. `otpCode`/`securityAlert`/`subscriptionConfirmation`) · emailService (legacy match/chat/subscription notices — now **delegates to `email.js`** so it also ships via Resend) · compatibility (score + Vedic Ashtakoot 27-nakshatra/8-guna/dosha; `resolveNakshatra()` 50+ aliases) · numerology (life-path from DOB + pairwise match; in `horoscope-match`) · profileCode (deterministic `TCS-XXXXXXXX` from userId, no DB column; powers `/search/by-code`)
- `validators/index.js` all express-validator schemas

**Auth:** httpOnly accessToken(15m JWT)+refreshToken(7d hashed). Rotation+family revoke. Lockout 5/30min (Redis). Google `POST /auth/google`. Mobile biometric→refresh-token flow.
**Limiters:** api 200/15m · auth 5/15m · signup 3/hr · pwReset 3/hr · search 30/min · message 60/min · profileUpdate 10/min · matchAction 60/min · upload 20/hr · admin 100/min · payment 10/hr
**Plans (REGULAR ladder, `utils/razorpay.js` PLANS):** free ₹0 · basic_premium ₹1299/30d/5 · premium_plus ₹2499/90d/15 · elite ₹3999/180d/30 · vip ₹5999/360d/unlimited+boost · nri ₹9999/180d/unlimited
**LAUNCH OFFER — SINGLE PLAN (live, admin-editable — `utils/launchOffer.js` + AppSettings row `launch_offer`):** **ONE plan on sale: `premium_plus` "Premium" ₹1,099 / 90d / UNLIMITED unlocks** (MRP anchor ₹2,499 = the regular 90d price, 56% off). basic/elite/vip/nri **withdrawn** — withdrawn means `createOrder` 400s `That plan is not available right now`, not merely un-rendered; `getPlanDetails` still RESOLVES them so existing holders/invoices/webhook keep working. Bundles ₹199/3 + ₹549/10, **bundle_25 withdrawn**. **Which cards show is an admin toggle** (Admin → Pricing & Offers, `/admin/launch-offer`, "On sale" per tier) — **ceiling 6 cards = the 5 paid tiers incl. NRI + Free** (`MAX_VISIBLE_PAID_PLANS=5`); `saveOffer` REFUSES a save with every tier withdrawn (nothing to buy) and the editor blocks it client-side too. Overlay lives INSIDE razorpay.js so `getPlanDetails`/`createOrder`/webhook/Google-Play/admin-grant all resolve one price. **Fails closed to regular pricing** (unset/malformed/expired/disabled). Sync reads off an in-process cache warmed by `initLaunchOffer()` in server.js, rewritten on every admin save, 60s revalidate. Migration **000057** rewrote the persisted row (defaults alone only apply on a first-ever boot); previous ladder snapshotted to AppSettings key `launch_offer_plans_pre_single` and restored by its `down`. Migration **000058** re-priced ₹1,199→₹1,099 in the persisted row (touches only `plans.premium_plus.amount`, and only if it still reads 119900 — a hand-priced row is left alone).
**Founding grant (OPEN):** 30 days · **3 unlocks** · cap 500 · per-member term (signup+grantDays, clamped to the window deadline), settings-backed with env fallback.
**Anti-harvest:** unlimited tiers (vip/nri) now have a rolling-24h unlock ceiling `UNLIMITED_DAILY_UNLOCK_CAP` (default 25) — a cheap unlimited plan was otherwise a scriptable export of every phone number.
**Migrations:** 000001–000060. `npm run migrate` (backend/) before prod (prod also auto-runs pending migrations on boot via server.js umzug). (`quizAnswers` JSONB IS created by migration 000028 — no manual ALTER needed.) 000035 ProfileView (viewerId,createdAt) index for recently-viewed; 000036 SuccessStories table; 000037 Profile.videoIntroUrl (video intro); 000038 search-index tuning (income/height/manglikStatus btree + interestTags GIN; drops duplicate Messages index); 000039 family-group chat (Groups/GroupMembers/GroupMessages + indexes); 000040 ContactMessages table (public contact form); 000041/000042 onboarding (nullable gender/dob + authoritative onboardingComplete flag); 000053 AppSettings (key/JSONB — admin-editable launch offer + founding window); 000054 ContactMessages.replyBody/repliedAt/repliedBy (admin support replies).

## API (`/api/v1` unless noted; full: `docs/06_API_Reference.md`)
- **auth** `/auth`: signup, login, refresh, forgot-password, reset-password, google, send-otp, verify-otp, GET me, logout, logout-all, change-password, GET sessions, DEL sessions/:id, DEL account
- **profile** `/profile`: GET/PUT me, GET me/stats, GET me/viewers, GET me/recently-viewed (all tiers), DEL me/photo, DEL me/profile-photo, POST/DEL voice-intro, POST/DEL video-intro, PUT privacy, GET :id, POST :id/unlock-contact, GET :id/compatibility, GET :id/horoscope-match (incl. `numerology` life-path block), GET :id/horoscope-match/pdf (premium — Kundli match-report PDF)
- **groups** `/groups` (auth, membership-gated): POST / (create), GET / (my groups), GET :id, DEL :id, POST :id/members, POST :id/invite (by userId/phone), DEL :id/leave, DEL :id/members/:userId, GET :id/messages, POST :id/messages, PUT :id/messages/:mid, DEL :id/messages/:mid
- **calls** (web in-browser, Agora) `/calls`: GET agora-token, POST initiate, PUT :id/accept|decline|end, GET history — web `CallProvider`+`CallOverlay` use agora-rtc-sdk-ng (lazy-imported), socket signaling; needs `VITE_AGORA_APP_ID`
- **search** `/search`: GET /, GET suggestions, GET by-code (public profile code `TCS-XXXXXXXX`→profile)
- **match** `/match`: POST :id {action}, GET likes, GET shortlist, GET mutual, GET daily (cached IST-day set, Redis TTL→midnight, free 5/premium 15)
- **success-stories** (public, no auth) `/api/v1/success-stories`: GET (published only)
- **contact** (public, no auth) `/api/v1/contact`: POST (contactLimiter 5/hr/IP, sanitized; stores ContactMessage + best-effort emails SUPPORT_EMAIL)
- **chat** `/chat` (premium): GET conversations, GET messages/:id, POST messages, POST send, PUT/DEL messages/:id
- **subscription** `/subscription`: GET plans, POST webhook, GET my-subscription, POST create-order, POST verify-payment, DEL current, GET history, GET invoice/:id
- **verification** `/verification`: GET status, POST submit (**selfie-only** — multipart `selfiePhoto`, govt-ID doc collection removed 2026-07-02; stale-client documentType tolerated but ignored. **Web captures the selfie LIVE via getUserMedia only — no file upload** (`components/verification/LiveSelfieCapture.jsx`), 2026-07-07), POST selfie (liveness video, RN). **Background-check tier REMOVED 2026-07-07** (routes/service/config/RN screen deleted; dormant `bgCheck*` DB columns left in place, no down-migration).
- **notifications** `/notifications`: GET /, GET unread-count, PUT read-all, PUT :id/read, DEL :id, POST/DEL fcm-token
- **calls** `/calls`: GET agora-token, POST initiate, GET history, PUT :id/accept|decline|end
- **guardian** `/guardian` (DB): GET my-guardians, POST invite, DEL :linkId, GET my-candidates, GET candidate/:id/matches|shortlisted, POST resolve-invite/:token
- **astrologers** `/astrologers`: GET /, GET my-bookings, GET :id, POST book, POST book/:id/verify-payment|start-call|end-call
- **block**/**report** `/block`,`/report`: POST :id, DEL :id, GET /
- **admin** `/admin`: GET/PUT launch-offer (pricing editor), GET contact-messages, PUT contact-messages/:id, **POST contact-messages/:id/reply** (emails the enquirer via Resend, records the reply, marks resolved — a FAILED send is reported and nothing is recorded), GET/POST users, GET users/:id, PUT users/:id/status|subscription, GET verifications, PUT verifications/:id {status,adminNotes}, GET analytics|revenue|reports, PUT reports/:id {status,adminNotes}, GET invoice/:id, GET/POST marketing-users, PUT marketing-users/:id/status, GET marketing-users/:id/stats, GET/POST referral-codes, PUT referral-codes/:id/toggle, GET leads, GET/POST success-stories, PUT/DEL success-stories/:id, POST push-smoke-test
- **marketing** `/api/marketing` (marketing role): GET dashboard|leads, PUT leads/:id/status, GET/POST referral-codes

**Flows:** Login→httpOnly cookies→`/auth/me`. Onboarding 14-step→`PUT /profile/me`. Match `POST /match/:id {action}`→mutual→notify. Chat premium+mutual (REST+Socket). Payment Razorpay order→verify→webhook fallback. Photo Multer→Cloudinary 500² face+1200² gallery max6. Boost +8 sort. Verify docs→admin→badge.

## Frontend (web)
`main.jsx`→`HelmetProvider`→`App.jsx`→AuthProvider→OnboardingProvider→SocketProvider→CallProvider. Contexts `useAuth/useOnboarding/useSocket/useCall`. `CallOverlay` mounted globally; in-browser voice/video via `context/CallContext.jsx`+`utils/agoraEngine.js` (agora-rtc-sdk-ng lazy import). Aliases `@`→src,@components/@pages/@context/@api/@utils/@hooks/@assets. HTTP `api/axios.js` (withCredentials, auto 401→refresh queue); `apiClient.js` alias. **react-router v7** (component/hook API only). Login/signup return full user (Profile) → no double `/auth/me`. **SEO:** `components/common/Seo.jsx` (react-helmet-async) sets per-route title/description/canonical/OG on public pages; `public/robots.txt` + `public/sitemap.xml` present.

Routes:
- public: `/ /login /signup /onboarding /forgot-password /reset-password /terms /privacy /about /contact /safety /help /success-stories` (`/help` = Help Centre, 2026-08-20: self-serve FAQ + support channels; linked from footer + Settings → More)
- ProtectedRoute: `/dashboard /profile /profile/edit /profile/:id /search /matches /chat /subscription /payment/success|failed|history /settings /notifications /verification /guardian /astrologers /astrologers/bookings /astrologers/:id` (`/matches` = Saved/Mutual/Likes-You tabs, added 2026-07-07)
- AdminProtectedRoute `/admin/*`: dashboard, users(+create,:id), verifications, subscriptions, **launch-offer (Pricing & Offers)**, revenue, reports, contact-messages (Support Inbox — now with in-product reply), marketing-users(+:id), referral-codes, leads, success-stories
- **i18n (web):** react-i18next en/hi/pa; config `src/i18n/`, `LanguageSwitcher` in Settings. Global Navbar + Login + Verification/Guardian/Astrologers/SuccessStories use `useTranslation` (`navbar`/`auth`/feature key namespaces). Remaining content pages (Home/About/Contact/Safety/Privacy/Terms) still English-only.
- MarketingProtectedRoute `/marketing/*`: dashboard, leads, referral-codes

Onboarding (web, 2026-06-30 redesign): **self-signup = 2 steps** — `CreateAccountStep` (combined: smart email/phone identifier `SmartContactField` + password + Terms + inline Send-OTP/verify via `OtpBoxes`; account created only after verify) → `BasicInfoStep`. Rest of profile filled later via edit-mode + Dashboard completion nudge. Guardian (`create_for_other`) flow keeps the longer path: CreatingFor→CreateAccount(legacy multi-field)→BasicInfo→…→Verification. `OnboardingContext` uses one canonical `phone` field (no more `phoneNumber` re-entry); STEPS-array `showIn` gates which steps render per mode.
Dead files removed (`pages/Profile.jsx`, `pages/Signup.jsx` deleted). Build: **Vite 8** (rolldown) — `manualChunks` is function-form; terser drops console in prod; chunks vendor-react/ui/utils; es2020. AdminProtectedRoute + ProtectedRoute accept `['admin','super_admin']`.

### Design System (2026-06-23 redesign)
Hifi web redesign handoff (Claude Design) lives at **`docs/design-handoff/`** — static HTML mockups (1 per screen) + `Design System.html` (canonical components, source of truth) + `README.md` (per-screen spec) + `ts-system.css` (token reference). Open any `.html`, use top harness bar to toggle viewport/theme/elder/state. **Mockups are reference, NOT code to copy** — recreate in React using existing Tailwind classes + `index.css` CSS vars. Tokens are unchanged (burgundy `#8B2346` accent-only never flat fills, gold `#C9A227` premium/VIP only, Playfair headings + Inter body).
- **Shared pieces (build/use first):** `components/common/SectionHeader.jsx` (tick bar — gold=premium, burgundy=standard — + Playfair title + count chip + subtext) · refined `components/ui/Avatar.jsx` fallback (solid `bg-primary-100` + Playfair initials `text-primary-700` + success verified badge; replaces old pastel `from-primary-100 via-gold-50` gradient EVERYWHERE) · one muted `bg-neutral-100` info panel (kill rainbow info boxes).
- **Themes:** light/dark via `html.dark` (Tailwind `darkMode:'class'`). **Elder mode** = `html.elder` class → base font 16→18.5px + muted text darkens (AA+), hit targets ≥44px, layout unchanged. Both toggled in Settings/theme system.
- **Every data view ships 4 states:** default · loading (shimmer skeleton matching layout, not spinner) · empty (icon + 1 line + CTA) · error (icon + retry). Premium-gated views add gold lock (blur + unlock CTA).
- **Redesign progress tracker:** `docs/design-handoff/PROGRESS.md`.

## Mobile (`mobile/`) — RN
**Stack:** Expo SDK51 · React 18.2 · RN 0.74.5 · react-navigation **v6** · react-native-screens 3.31.1 · MMKV **v2** · old arch · Zustand+React Query · i18n en/hi/pa. Docs: `docs/01–06`.
**Nav:** RootNavigator→(Auth|Onboarding|Main). MainNavigator role-gates AdminStack (admin/super_admin), BureauStack (bureau). IncomingCallModal inside NavigationContainer, outside Stack.

Feature areas (`mobile/src/features/`):
- **auth** Splash, Welcome, Login (biometric+429 lockout), Signup (strength bar), Forgot/ResetPassword, OTP (4-box)
- **onboarding** 14 steps mirror web; OnboardingContext resume, OnboardingLayout progress
- **home** feed, completeness strip, quick actions, new profiles
- **profile** OwnProfile (gallery/ring/badges/preview/milestone/quiz CTA), ProfileDetail (sticky bar, accordions, compat→breakdown sheet, horoscope→Ashtakoot, voice playback, ⋮→BlockReportSheet), EditProfile, SelfieVerification, BackgroundCheck, Verification (4 tiers)
- **search** Search (infinite/sort/saved), FilterPanel (@gorhom sheet, ranges, gotra exclude, manglik, save), ProfileCard, CompatibilityMeter, VerificationBadges
- **matches** 4 tabs (Mutual/Shortlisted/LikedMe/MyInterests), OfflineBanner (useOfflineShortlist MMKV)
- **chat** Conversations (Plus+ gate), ChatThread (inverted, receipts, typing, optimistic, edit/del), FamilyGroups, FamilyGroupChat
- **calls** Voice, Video (draggable PiP), IncomingCall (30s). Agora dynamic require
- **subscription** Subscription (plans, Razorpay stub, history), AstrologerMarketplace, AstrologerDetail
- **notifications** Notifications (infinite, mark-read, deep-link), Settings (incognito, elder mode, language, dark mode, delete account, guardian/family/astrologer)
- **admin** AdminHome, VerificationQueue, ReportsQueue
- **bureau** BureauHome, ClientRoster, MatchProposal (3-step), Earnings, Support, SuccessStory
- **guardian** GuardianSetup, GuardianView (RO), GuardianCandidates
- **horoscope** HoroscopeMatch (guna bars/dosha/manglik), CompatibilityBreakdownSheet, Quiz (10q)

Stores/hooks: authStore, uiStore (darkModeOverride null/true/false), callStore · useSocket (singleton, AppState reconnect) · useNotificationHandler (FCM dynamic) · useOfflineShortlist · useTheme `{isDark,c}` · elderTheme.
Native modules (dynamic require, stub in Expo Go, need native build): Agora, Razorpay, Firebase messaging, expo-camera/av/document-picker/local-authentication.

**Gotchas:**
- tsc in PATH=v4 → use repo `node_modules/.bin/tsc -p tsconfig.json` from `mobile/`
- `RouteProp` from `@react-navigation/native` (not native-stack)
- MMKV v2 `new MMKV({id})`; cache typed accessors `.setString/.getString`
- `@shared/*` alias in tsconfig paths AND metro.config
- ProfileSummary no `age`/`name` → age from `dateOfBirth`; name `[firstName,lastName].filter(Boolean).join(' ')`
- `Match.MatchedProfile` not `match.profile`
- no `colours.text`/`.surface` → `textPrimary`/`surfaceCard`; no `borderRadius['2xl']` → literal 20
- `VerificationTier` is interface not union → use `DocumentType` for maps
- tab nav from stack: `nav.navigate('MainTabs',{screen:'X'})`; elder mode hides Chat tab (guard `!elderMode`)
- admin routes: PUT `/admin/verifications/:id`, GET `/admin/analytics`, PUT `/admin/reports/:id`; FCM `/notifications/fcm-token` (not auth/device-token)
- Razorpay mobile: set `EXPO_PUBLIC_RAZORPAY_KEY_ID`; AuthUser no `name` → prefill `user.email`
- `getAshtakootScore` null when both nakshatras unknown → fall back rashi→empty; HoroscopeMatch params `{userId,name}` (full name)
- `queryKeys.myProfile` = `['profile','me']`

## Env
**Backend** `.env.development` (root; loaded by `NODE_ENV=development`). Req: `PORT=5001 DB_HOST DB_PORT DB_USER DB_PASSWORD DB_NAME JWT_SECRET FRONTEND_URL`. Status: Cloudinary✅ **SMS/MSG91✅ (local, IP-whitelisted)** Razorpay❌ Email❌ GoogleOAuth❌ Redis optional. Feature: `SMS_PROVIDER=msg91 SMS_API_KEY=<authkey> MSG91_TEMPLATE_ID=<id> SMS_SENDER_ID=TRISHD` (MSG91 authkey IP-whitelist must include the egress IP — dev `49.43.105.159`, prod VPS `178.16.138.82`) · `AGORA_APP_ID+AGORA_APP_CERTIFICATE` · FCM creds. (BG_CHECK_* removed 2026-07-07.)
**Frontend** `frontend/.env`: `VITE_API_URL VITE_WS_URL VITE_RAZORPAY_KEY_ID VITE_CLOUDINARY_CLOUD_NAME✅ VITE_GOOGLE_CLIENT_ID❌ VITE_AGORA_APP_ID` (for web calls; calls UI auto-hides when unset).
**Mobile** `EXPO_PUBLIC_*`. Bundle IDs `com.tricityshadi.app` (iOS+Android). Set eas.json submit.production.ios (appleId/ascAppId/appleTeamId) before `eas submit`.

## Deploy
```bash
docker-compose up -d
docker-compose --profile full up -d                    # prod
docker-compose --profile full --profile monitoring up -d
```
Nginx upstream `backend:5000` (host maps 5001:5000). **CRITICAL:** `--force-recreate` when rebuilding backend (plain up won't replace).
Pre-launch `bash scripts/prelaunch-check.sh` (`ENV_FILE=.env BASE_URL=https://tricitymatch.com`). Load `k6 run scripts/load-test.js --env BASE_URL=...`. FCM smoke `POST /api/v1/admin/push-smoke-test`.
Checklist: real Razorpay → Email → Google OAuth → `.env.production` → strong secrets → migrate(→000039) → seed admin → PWA icons → HTTPS → SMS/Agora/BG_CHECK/FCM env (+ `VITE_AGORA_APP_ID` for web calls).

### VPS / Server access
Production VPS: `178.16.138.82` (Ubuntu 24, root). Key-only SSH (no private key or password in repo — keys live in `~/.ssh/`):
```bash
ssh tricityshadi-vps          # alias in ~/.ssh/config
ssh -i ~/.ssh/tricityshadi_vps root@178.16.138.82   # explicit
```
Pubkey installed in server `~/.ssh/authorized_keys` (comment `claude-code@tricityshadi-vps`). To revoke: remove that line on the server + `rm ~/.ssh/tricityshadi_vps*` locally. Rotate any password-auth creds; prefer disabling `PasswordAuthentication` in `sshd_config` now that key auth works.

> ⚠️ **SHARED VPS — multiple production sites on this box. Do NOT disrupt co-tenants.** Other sites hosted here: **cityfreshkart, college-placements, ecom, edumapping.com, school.globoniks.com, tricitylifeinsurance** (nginx sites + their own docker/services). Hard rules: scope every command to TricityMatch only — never global `docker stop/rm/prune`, never `docker compose down` from a shared dir, never blanket `systemctl restart docker`. nginx: only edit the `tricitymatch.com` + `tricityshadi.com` site files; `nginx -t` before any `reload` (a bad global config breaks ALL sites). Don't reuse ports already bound by others (in use: 3000/3001/3002/5000/5001/5002/5003/5006/9000/9001/80/443).
>
> **TricityMatch footprint on the VPS:** docker containers `tricitymatch-frontend` (host **3002**→80), `tricitymatch-backend` (host **5002**→5000), `tricitymatch-db` (postgres:15-alpine), `tricitymatch-redis` (redis:7). nginx sites: **`tricitymatch.com`** (primary, `sites-available` + symlink) and `tricityshadi.com` (legacy — 301s page routes to the new domain but still proxies `/api/` + `/socket.io/` for shipped mobile builds; `sites-enabled` regular file, not a symlink). webroot `/var/www/tricitymatch`. Note prod host ports are **3002/5002** here (not the local 3000/5001). Manage only via this project's compose file; target containers by `tricitymatch-*` name.

## Admin
`admin@tricitymatch.com` / `Pass@1234` (or `ADMIN_EMAIL`/`ADMIN_PASSWORD`). Login `/login` (not /admin/login). Re-seed `node backend/seeders/adminSeeder.js`.

**Prod QA member account** (real prod member on user's own inbox — use for live prod testing / receiving OTP + emails): `globoniksprod@gmail.com` / `TricityQA@2026` (userId `ba32aefe-e5b2-4089-a963-0cc7bd5c4e37`). Free tier, has test profile data. OTP/verification emails land in that Gmail.

## Testing
**QA playbook: `docs/QA.md`** — universal, reusable: method + severity/status legends, env/creds, per-screen + anti-slop checklists, web (375/768/1440) + admin + **RN apps iOS+Android simulator** plan, readiness gates. (Replaced the scattered root QA files — `browser-qa-*`, `qa-progress`, `mobile-qa-progress`, `bug-tracker`, `production-bugs`, `launch-readiness`, `release-*`, `user-flows`; their history is in git + Audit History below. Audit-phase tracker `review-progress.md` stays.)
Backend unit+integration **Jest 30**+Supertest `backend/tests/` (**116 unit green**: numerology, profileCode, sanitize, validators, errorHandler, notifyUserImports, razorpay [timing-safe], bgCheckWebhook [fail-closed], **uploadMagicBytes** [SEC-4], **groupAuth** [membership IDOR gate]; integration: auth — needs live DB). Frontend **Vitest 4**+RTL 16 `frontend/src/tests/` (**31 green**: utils/validators + components/routeGuards). E2E Playwright `e2e/tests/` (9).
**Jest 30 note:** `--testPathPattern` renamed → `--testPathPatterns`.
✅ The 9 previously-stale unit tests are FIXED (errorHandler realigned to shipped API + AppError default 500; searchValidation cross-field age/height validators). Backend unit suite now 116/116 green.

## Known Issues
**Config-gated (need real creds, not code):** 🔴 Razorpay placeholder · ✅ **Email LIVE via Resend** (2026-07-01 — `RESEND_API_KEY`, `from`=`noreply@tricityshadi.com` on Resend-verified domain; covers OTP/reset/verification/security-alerts/welcome/digest/notices. The old `sendEmail({to,…})` vs `(to,template,data)` signature mismatch is **FIXED** — `sendEmail` now accepts both shapes + a single-object shape. **Provider routing** in `utils/email.js`: `channel:'transactional'`→Resend-first, `channel:'documents'` (invoices/receipts/admin/support)→SMTP-first, each falls back to the other configured provider so documents ship via Resend until SMTP is wired. SMTP creds still TODO for the documents leg.) · 🟡 GoogleOAuth off · ✅ SMS OTP live (MSG91 4-digit; needs egress IP whitelisted on MSG91 authkey) · 🟠 Push stub (FCM creds+native build) · 🟠 Redis not in dev.
**Incomplete features:** ⚪ full web i18n partial (global Navbar + Login + auth/navbar keys done ×3 langs; remaining pages English) · ⚪ web calls need `VITE_AGORA_APP_ID` + Agora server creds to function (code complete, config-gated). **Done this pass:** family-group chat backend (was disabled→built+membership-gated) · kundli PDF · web in-browser calls (Agora) · SEC-4 full magic-byte.
**Held dep migrations (breaking, no security benefit — audits clean):** ⚪ react 18→19 BLOCKED (mobile/RN pins react@18.2.0 → dual-React; root override pins 18.2.0) · ⚪ tailwindcss 3→4 (config rewrite) · ⚪ express 4→5 · ⚪ multer 1→2 · ⚪ umzug 2→3 (server.js v2 API) · ⚪ eslint 8→10 (flat config).
**Minor:** ⚪ `emailService.js` legacy (chat+match) · ⚪ admin user detail "No profile created yet".
**Mobile — missing backend (features hidden/disabled gracefully in RN until server side ships):** ⚪ **saved-searches** (no `/search/saved` route — Save-search UI hidden in `FilterPanel`/`SearchScreen`) · ⚪ **sent-interests** "My Interests" tab (no `/match/sent` — tab removed; backend exposes only daily/likes/shortlist/mutual) · ✅ **RN verification CLOSED (verified 2026-08-18)** — member submit is selfie-only via LIVE front camera (`ImagePicker.launchCameraAsync`, no library picker), posts `selfiePhoto` multipart; badge row reads real state (`ec74842`); admin queue is selfie-vs-profile-photo (`a6b0dd0`). The old "stale ID-tier submit" note was obsolete · ✅ **astrologer marketplace + detail** de-stubbed 2026-08-16 (`0498899`) — `/astrologers`, `/astrologers/:id` and `/astrologers/book` all work and the dev DB **is** seeded (the old "empty/unseeded" note was wrong); `STUB_ASTROLOGERS` + the invented ratings/slots/reviews/certification are gone, detail screen reads `getAstrologer(id)` with real loading/error/empty states. Still ⚪: no dedicated **my-bookings** screen, and consultation calls need Agora creds · ⚪ **kundli upload** (onboarding Step3) removed — no kundli-storage backend (multipart field was silently dropped) · ✅ **success-stories browse** DONE 2026-06-21 (`SuccessStoriesBrowseScreen` reads public `GET /success-stories`, linked from Settings) · ⚪ RN i18n residual: 6 member screens still hardcoded EN (Notifications, Astrologer×2, HoroscopeMatch, Support, SuccessStory); admin/bureau/call screens EN (out of scope). Language switch works end-to-end (verified live hi/pa).
**Dev-seed data quirks (NOT app bugs — affect local QA only, prod uses real plans/Cloudinary):** ⚪ seeded premium test accounts had invalid planType `'elite'`/`'premium'` (not in `basic_premium|premium_plus|vip`) AND expired `endDate` → `requirePremium` 403s; fix the row (set planType `vip` + future `endDate`) for premium QA. ⚪ seed profile photos are relative `/uploads/*` paths with no files → `SmartImage` shows initials fallback (prod = absolute Cloudinary URLs).

## Audit History
- 2026-03-12 Security audit — all critical/high/med resolved, CORS hardened
- 2026-04-28 Backend hardening (9 bugs: CSP/webhook/email/notif/boost/validators); Workflow audit (11 flows, 8 fixed); Competitive features (religion/caste/income/motherTongue filters, photo blur, incognito, verified badge, weekly digest, VIP boost)
- 2026-04-30 UI fixes (dark mode, chat route, profile meter, password std, Google Sign-In)
- 2026-05-01 Backend prod hardening (16: webhook path, raw body, timing-safe sig, OTP limit, pagination, PII leak, XSS strip, startup validation)
- 2026-05-04 Prod QA (6 bugs: DB SSL crash, missing migrations, CORS loopback, login complexity, error display, 404 redirect)
- 2026-06-02 Mobile-web responsiveness (Home.jsx 375/768px)
- 2026-06-09 Mobile re-theme (neutral palette →web #FAFAFA/#FFFFFF/#E8E8E8/#2D2D2D, errorBg/warningBg/successBg/infoBg tokens, callTheme.ts dark-navy)
- 2026-06-14 Competitive parity R1 (benchmark vs Shaadi/Jeevansathi → `docs/07_Competitive_Benchmark.md`, spec `docs/08_Spec_Competitive_Parity.md`). Shipped: web pages for Verification/Guardian/Astrologers (booking-only, in-browser calls DEFERRED); GET /match/daily (cached IST set); GET /profile/me/recently-viewed; SuccessStory model+admin CRUD+public page (Home.jsx now fetches); web i18n scaffold en/hi/pa. Migrations 000035/000036 — run `npm run migrate`.
- 2026-06-14 Competitive parity R2 (re-benchmark → final buildable-gap closure; spec addendum C8–C10). Shipped: **search-by-ID** (`GET /search/by-code` + shareable `TCS-XXXXXXXX` code on profile, util `profileCode`); **video intro** (`POST/DEL /profile/video-intro`, migration **000037** Profile.videoIntroUrl, web `VideoIntroManager`+playback); **numerology** (life-path + pairwise match on `horoscope-match`, util `numerology`). Unit tests `numerology.test.js`+`profileCode.test.js`. Remaining gaps intentional: web calls, kundli PDF, full web i18n, SMS match alerts (won't-do — push+email cover), settlement guarantee, RM web. Run `npm run migrate` for 000037.

- 2026-06-16 **Full 9-phase audit + remediation** (tracker `review-progress.md`, 53 findings REF-1..53). Fixed all 3 Highs (WH-1 bg-check fail-closed+env guard; SOCK-1/MF-1 group-socket IDOR disabled; FE-1 super_admin) + ~all Mediums/Lows: UTIL-1 timing-safe verify, VAL-1 OTP validators, SEC-1 no query-token, WH-2/3/4, SEC-2 CORS, SEC-3 dead guard removed, SEC-4 resource_type pin, CTRL-1/2 incognito, PERF-1 search attrs, UTIL-2 indexed by-code, ERR-1/2/3/4, FE-2 single auth call, SOCK-3/5/6. Migration **000038** (search indexes + drop dup Messages index); 000036 made idempotent. SEO: robots/sitemap + per-route helmet meta (`Seo.jsx`). Tests: route-guard + razorpay + bgCheck-webhook suites.
- 2026-06-16 **Dependency security + update sweep.** REF-3 single root lockfile. Fixed all runtime vulns: ws DoS (8.21.0 override), cloudinary 2.10 (arg-injection), nodemailer 9 (SMTP-injection), dompurify 3.4.10 (XSS), react-router 7 (open-redirect XSS). Updated: vite 8/vitest 4/jsdom 29/RTL 16, framer-motion 12, lucide 1, react-icons 5, typescript 6, helmet 8, bcryptjs 3, jest 30, +safe minors. react pinned 18.2.0 (override, mobile dual-React). **Result: frontend audit 0 vulns; backend 0 high/0 critical** (27 moderate transitive). Held: react 19 (mobile-blocked), tailwind 4, express 5, multer 2, umzug 3, eslint 10.

- 2026-06-16 **Build-work completion pass.** Closed the buildable "incomplete" gaps. (1) **9 stale unit tests** fixed — searchValidation cross-field ageMin≤ageMax + heightMin≤heightMax custom validators; errorHandler tests realigned to shipped API (`error.code`, nested `{error:{}}` shape, config-mock for dev/prod stack), AppError default statusCode 500 (BE **116/116 unit** green). (2) **Kundli PDF** — `utils/kundli.js` (pdfkit) Ashtakoot+Manglik+numerology report, `GET /profile/:id/horoscope-match/pdf` (premium), web download button on ProfileDetail. (3) **SEC-4 full** — `validateUploadedFiles` magic-byte check on disk-stored uploads (wired profile-photo + verification-doc routes) + unit test `uploadMagicBytes.test.js`. (4) **Family-group chat backend** (REF-51) — Group/GroupMember/GroupMessage models, migration **000039**, `groupController`+`/groups` routes (membership-gated, IDOR-closed), socket join-group re-enabled with membership check + server-authoritative broadcasts, mobile `api/chat.ts`+screens rewired to `/groups`, unit test `groupAuth.test.js`. (5) **Web i18n** — global Navbar + Login wired to i18next, `navbar`/`auth` keys added en/hi/pa. (6) **Web in-browser calls** — `agora-rtc-sdk-ng` (lazy chunk), `CallProvider`+`CallOverlay`+`agoraEngine`, `api/calls.js`, voice/video buttons on ProfileDetail, socket signaling; config-gated on `VITE_AGORA_APP_ID`. Migration 000039 applied live.

- 2026-06-18 **Web mobile-view professionalism pass (@375px).** Tracker `mobile-qa-progress.md`. Screen-by-screen (Playwright @375, fix-on-discovery), web mobile view only — **RN apps deferred**. Removed "AI slop": all onboarding emoji + childish multi-color info boxes (`bg-blue/green/cyan/yellow/orange/indigo/pink/purple-50`) → one muted `bg-neutral-50` standard; `✓`/`!`/`✦` unicode-as-icon → `FiCheck`/`FiAlertCircle`; rose/pink/purple/amber→yellow gradients → solid brand (burgundy/gold). **MyProfileView + ProfileDetail were 100% rose-themed → recolored to brand burgundy** (`rose-`→`primary-`). Tier badges (Dashboard/ProfileCard) rose/purple→primary/gold. **Bugs fixed:** Terms/Privacy brand "TricityMatch"→"TricityMatch"; FilterPanel "Apply Filters" un-tappable on mobile (sheet z-index under BottomNav → z-[60]/[55]). Workflows re-verified (Contact submit, Search filter apply). Clean/no-fix: Home (✦/✓ are intentional royal typography), About/Safety/SuccessStories, Login/Forgot/Reset, Subscription, Settings, Verification, global chrome. FE 31/31 + build green.

- 2026-06-19 **Admin panel audit + brand/theme fix (@1440).** Walked all 10 admin routes (dashboard/users[list,detail,create]/verifications/subscriptions/revenue/reports/marketing-users/referral-codes/leads/success-stories) — all render + functional (tables, filters, tabs, CRUD modals, status dropdowns). Fixes: **brand** "TricityMatch"→"TricityMatch" + logo TC→TS (AdminLayout/Dashboard/Login); **off-brand theme** entire admin was rose (~47× across 12 files) → `rose-`→`primary-` burgundy; **accent blue** action buttons/links in marketing-side admin pages → primary (kept semantic `bg-blue-100` status chips); **UX** AdminUserDetail DOB raw-ISO → formatted. Build green, FE 31/31. Note: marketing-role portal (`pages/marketing/*`) still blue (separate portal, deferred); admin account name/email still "TricityMatch" (DB data).
- 2026-06-20 **RN apps simulator QA pass (iOS + Android dev builds).** First-ever live simulator QA — tracker `rn-qa-progress.md`. **Setup repaired** (root lockfile missing react-navigation/screens transitive deps `warn-once`/`query-string`/`color`/`use-latest-callback`/`react-freeze`; iOS pods; Android SDK+`qa_pixel` AVD bootstrap; hoisted-RN symlink). **Slop removed across 18 files** (emoji→Ionicons, pastel cards/gradients→brand burgundy/gold, `✓`/`✓✓`-as-text→icons, `🎉`/`👋` dropped) — verified live on Android (Welcome/Login/Step0/Home/Matches/Chat/Search/Filters/Profile). **7 functional bugs found via adb walkthrough + fixed:** (B1) Android status bar off-brand rose `#E11D48`→light, runtime `AppStatusBar` + native theme; (B2) 🔴 Search/all bottom-sheets crash — `@gorhom/bottom-sheet` v5 needs reanimated≥3.16 but SDK51 pins 3.10 → downgrade to **^4.6.4**; (B3) 🔴 returning users re-onboarded every login — backend never returned `onboardingComplete`; (B4) 🔴 Chat dead for everyone — `hasPlus=false` hard-coded in Conversations+Matches → `selectPlan!=='free'`; (B5) 🔴 backend never serialized `subscriptionPlan` (whole app saw every user as free) — added `withDerivedUserFields` (subscriptionPlan + onboardingComplete) to login/signup/getMe; (B6) 🟠 OwnProfile/Edit/Detail showed "undefined undefined"+0% — `getMyProfile`/`getProfile`/`updateMyProfile` returned the `{success,profile}` envelope not `.profile`; (B7) 🟡 CompletionRing rendered as starburst → fixed to 10-tick rim ring; (B8) ⚪ 5 pre-existing tsc errors (`onPrimary`/`letterSpacing` theme tokens) added. **Verified live:** Chat unlocks for elite user, Profile shows "Aman Singh/86%", Search+Filters work. Regression: BE **116 unit** + FE **31** green; mobile `tsc` **0 errors**. Open (noted, not fixed): relative `/uploads/*` avatar paths blank on native (seed/default-only; prod=Cloudinary absolute); "No profiles found" shows above populated cards. Backend `authController` change is additive (web unaffected). No commit.
- 2026-06-20 **RN member-app "make it work like the website" pass (API contract + logo + i18n + polish).** Root cause: the RN `api/*.ts` layer was never integration-tested — wrong **paths** AND wrong response **unwrapping** across the app. **Fixed every member API module** (paths + `{success,<key>}`→`.<key>` unwrap + types + screen consumers): `matches` (`/match/matches`→`/mutual`, `/shortlisted`→`/shortlist`, `/liked-me`→`/likes`, `/matches/feed`→`/match/daily`), `chat` (`/chat/${id}`→`/chat/messages/:id` for thread/edit/delete; `getConversations`→`.conversations`), `search` (cursor→page + `{profiles,pagination}`; fixes "15 profiles found" + infinite scroll), `subscription` (`getPlans` object→array — was crashing the Plans tab like Search; `verifyPayment`→`.subscription`, history→`.subscriptions`, `createOrder` `.order.id`), `notifications` (cursor→page + unreadCount), `verification` (`/verification/me`→`/status` + tier mapping + upload now sends `documentType`+`documentForward`), `guardian` (whole module: real paths `/my-guardians`/`/my-candidates`/`/candidate/:id/matches|shortlisted`, **email-based** invite not phone, minimal-shape→ProfileSummary mappers), `calls` (`getCallHistory`→`.calls`), `astrologers` (`.astrologers`/`.bookings`). **Verified live on Android with real data**: Home "Today's Matches" populated, Search "15 profiles found"+cards, Matches 3 tabs, Chat empty-state, Profile "Aman Singh/86%", Settings "Current: elite", EditProfile real prefill. **Dead-ends hidden** (no backend): sent-interests "My Interests" tab removed; save-search UI hidden; education/income verification tiers → "Coming soon" — all documented in Known Issues. **Real website logo**: PNG exported from `frontend/public/images/logo.svg` via sharp → `mobile/assets/{logo,icon,adaptive-icon,splash}.png`; `Logo.tsx` now `<Image>` the real TS serif mark (replaced hand-built "TS" text badge) — verified live on Welcome. **Language switching verified working end-to-end** (Login + Welcome render full Hindi); added missing `welcome.*` keys ×3 langs (en/hi/pa). **Avatar polish**: shared `SmartImage` (resolves relative `/uploads/*` → API host + initials fallback on missing/error) wired into `ProfileCard` (Search/Matches) + Home cards — blank grey boxes → burgundy initials, killed the broken-image LogBox spam. Regression: BE **116 unit** + FE **31** green; mobile `tsc` **0 errors**. Scope: member app only (Admin/Bureau stacks deferred). No commit.
- 2026-06-21 **"Make apps work like the website" — commit + finish parity (committed to `main`).** Committed the whole prior uncommitted RN pass (~95 files) then closed the remaining gaps with a live Android dev-build walkthrough. **API contract (modules the prior pass missed):** `auth.ts` `/auth/refresh-token`→`/refresh`, `getMe` unwrap `.user`, `deleteAccount`→`/auth/account`, read `tokens.{accessToken,refreshToken}` from body (drop dead `/auth/device-token` → FCM lives in `notifications`); `client.ts` interceptor same `/refresh` + persist rotated refresh token; `profile.ts` `uploadPhoto`→multipart `PUT /profile/me`, `deletePhoto` by `photoUrl`→`DELETE /profile/me/photo`; `block.ts` report→`POST /report/:userId {reason}`. **Backend (minimal, additive — web unaffected):** login/signup/refresh/google now return `refreshToken` in body (+ `user` on refresh) so native clients persist + cold-start restore (verified live: app auto-relogins after force-stop); public `POST /success-stories` (rate-limited, creates `draft` for admin review) wires the previously-dead RN submit screen. **🔴 Real bugs found in live QA (affect prod, incl. web):** `chatController.getConversations` used `= ANY(:ids)` with Sequelize replacements → `malformed array literal` crash whenever a user has mutual matches → **`IN (:ids)`** (web chat hit this too); RN `chat.ts`/`matches.ts` returned backend shapes the screens didn't read (conversations `user.name`→`profile`; flat match items→`{MatchedProfile}` — Matches showed "Unknown") → mappers added; `subscription.getPlans` spread the marketing-only API object over `planType`, shadowing shared `PLANS` capability flags → **every plan showed all features excluded (even VIP)** → use shared `PLANS` + overlay live price. **Visual parity (native idioms ~85-90%):** gradient `Button` (primary + new `gold`) via `expo-linear-gradient` (added; needs native rebuild + restore `mobile/node_modules/react-native` symlink after `expo install`); ProfileCard burgundy shadow + photo scrim + gold high-compat star + score-coloured bar; CompatibilityMeter green/gold/burgundy by score; Subscription "Most Popular"/"Best Value" floating badges; remaining raw `<Image>` avatars (Matches/Chat/OwnProfile) → `SmartImage`; web drive-by `ProfileCompletionMeter` rose `#be123c`→brand. **Dead-ends removed:** Step3 kundli upload (no backend). **Verified live on Android:** Welcome/Login (gradient btns)→cold-start session restore→Home (VIP, 86% ring, matches)→Search (15 found, scrim+compat colour)→Matches (3 tabs, real names)→Chat (list+thread+send)→Settings→Subscription (badges + correct per-plan features). Regression: BE **116 unit** + FE **35** + mobile `tsc` **0**. Scope: member app only (Admin/Bureau deferred); decisions = native-idiom parity + mobile→web only. See `rn-qa-progress.md`.
- 2026-06-21 **Remaining mobile→web parity gaps closed (committed to `main`).** Explore sweep + verification found 4 member workflows on the website but missing/stubbed on mobile, all backed by shipped endpoints. Built (member app, native idioms): (1) **Profile Visitors** + **Recently Viewed** horizontal rails on OwnProfile (mirrors web Dashboard) — `GET /profile/me/viewers` (premium, upsell for free) + `/profile/me/recently-viewed` (all tiers); tap→ProfileDetail. (2) **Privacy Controls** screen replacing the dead `Alert('Coming soon')` — visibility segmented (everyone/matches_only) + online-status + last-seen, hydrated from `getMyProfile`, saved via `PUT /profile/privacy`. (3) **Success Stories browse** (`SuccessStoriesBrowseScreen` reads public `GET /success-stories`, linked from Settings; submit via header +). New `api/profile.ts` fns: `getProfileViewers`/`getRecentlyViewed`/`updatePrivacy`/`getSuccessStories`. **🔴 Real bug found + fixed (affected web too):** migration 20240101000014 added `profileVisibility`/`showOnlineStatus`/`showLastSeen` to Profiles, but the **Sequelize model never declared them** → `PUT /profile/privacy` echoed in-memory values that `save()` silently dropped and `GET /profile/me` never serialized → privacy settings never persisted on web OR mobile. Declared the 3 columns on `models/Profile.js` → now persists + round-trips (verified live: set→reload→hydrates server state). **Verified live on Android (seeded VIP):** Visitors rail (Rohit/Arjun/Karan) + Recently-Viewed (Simran/Priya) render + navigate; Privacy saves + hydrates; Success Stories list renders a published story. Regression: BE **116 unit** + FE **35** + mobile `tsc` **0**. Scope: member app only. Out (verified not buildable): saved-searches/sent-interests (no backend), education/income verification tiers (enum ID-only), astrologer reviews/slots stubs, horoscope-match PDF (deferred). See `rn-qa-progress.md`.
- 2026-06-29 **RN full visual + HCI QA sweep (iOS + Android dev builds, fix-on-discovery; member app).** Complete-coverage pass (`rn-qa-progress.md` Round 3). Drove Android via `adb input`+`uiautomator` text-dumps + iOS `idb`/`simctl`; live screenshots then text-dump+`logcat` crash-smoke. **3 new bugs found + fixed (4 mobile files, `tsc` 0):** (R3-1) 🔴 **haptics crash on every button press (dev)** — static `require('expo-haptics')` (module not installed) emits Metro "Requiring unknown module" redbox on first haptic call, blocking the UI; rewrote `utils/haptics.ts` to use `expo-modules-core` `requireOptionalNativeModule('ExpoHaptics')` (null-safe, no static require → no overlay; real haptics auto-enable after `expo install expo-haptics`+rebuild). Prod was a silent no-op but dev/QA was blocked. (R3-2) 🟠 **signup/reset password rule understated** — placeholder "Min. 6 characters" + client `<6` vs backend **8+complexity** → valid-looking passwords 400'd; aligned client validation + placeholder + `passwordRules` to 8+upper/lower/digit/symbol in `SignupScreen`+`ResetPasswordScreen`. (R3-3) 🟡 **paid-tier plan badge not gold** — OwnProfile plan chip burgundy for all tiers; made tier-aware (paid→`goldSoft`/`secondary` gold, free→burgundy+Upgrade). **Then a live new-user walk (cleared app → signup) exposed 3 more, incl. a critical:** (R3-4) 🔴 **RN signup 100% broken** — `signupValidation` hard-required `firstName`/`lastName` but RN posts only `{email,password}` (name collected in onboarding) → every registration 400'd (never caught: prior passes logged in with the seeded account). Made firstName/lastName `.optional` + Profile.create defaults `''`; web unaffected (still sends names). (R3-5) 🟠 **new signup skipped onboarding → empty Home** — `onboardingComplete` derived from `gender&&dateOfBirth` but signup seeds NOT-NULL placeholder defaults; re-keyed it off **firstName** (empty until onboarding Step 1). (R3-6) 🟡 onboarding Step 1 DOB showed garbage "01T00:00:0…" — `Step1Screen` split a full ISO ts; normalise to date-only. All verified live: fresh Android signup → 201 → onboarding Step 0→1 (DOB `01/01/2000`)→2 (religion picker), validation gating + pickers crash-free. **Walked clean (live):** Welcome/Login/Signup/Forgot, Home, Search+FilterPanel, Matches×3 (color-coded compat), Chat (Conversations/Thread/Groups), OwnProfile (Visitors+Recently-viewed rails, ring, inline-edit), EditProfile, Notifications, Settings (sectioned, "Current: vip"), ProfileDetail+CompatibilityBreakdownSheet, Verification, PrivacySettings, Support, Success Stories. **Elder-mode round-trip re-verified** (Chat tab hide 5→4 / show); dark+elder toggles crash-safe; `logcat` clean app-wide. Safe-area solid (OnboardingLayout `SafeAreaView` + auth `paddingTop`); no emoji/off-brand hex in member JSX. Stale "It's a Match! 🎉" = 6-day pre-A5 row (A5 holds). **Not live-smoked (env-blocked, code-clean):** onboarding live walk (iOS Strong-Password modal blocks idb signup; Android fresh-signup would lose VIP session), Calls/SelfieVerification (Agora/expo-camera not in dev binary — config-gated), Guardian/Astrologer/Quiz/HoroscopeMatch (reachable, consistent clean pattern). Regression: mobile `tsc` **0**; backend **unit 116/116** (additive, web-safe signup change); FE untouched. Scope: member app (Admin/Bureau deferred).

- 2026-06-30 **Web signup redesign + funnel QA sweep + OTP correctness (committed: NO — uncommitted).** Scope = web member funnel (mobile untouched). **(A) Onboarding/signup rebuilt for conversion** (`frontend`): killed the duplicate-contact UX the user flagged. Was: account step collected `phone`, then a separate Verification step re-asked the SAME number into a different empty field `phoneNumber` (pure re-entry), plus a standalone Terms-only "Welcome" step (dead gate). Now **single canonical `phone`** end-to-end (removed `phoneNumber` from `OnboardingContext`), Terms folded into the account step, Welcome step deleted. Then **combined account+verification into ONE page** (`CreateAccountStep` branches on `mode`): one **smart identifier box** (`SmartContactField` auto-detects email vs 10-digit mobile, shows live **+91** pill) + password + Terms + inline **Send OTP → 6/4-box OTP (`OtpBoxes`, magic-mcp-inspired, dependency-free) → verify**; can't pass step 1 until verified, so **the account is only created after verification**. Signup is now **2 steps** (combined → Basic Info); `BasicInfo` modernized (side-by-side names + segmented gender chips). Guardian (`create_for_other`) flow kept its legacy multi-field form + separate Verification step. **(B) OTP backend correctness** (`authController`): (1) 🔴 **email OTP verify was fully broken** — `verifyOtp` ran every target through `smsService.verifyOtp`→`normalizePhone`, so an email normalized to `null` and threw *before* any code/bypass check; rewrote the email branch to verify against its own `otp:<email>` store. (2) **Server-side "verified" proof** — `verify-otp` now sets a 30-min `otp-verified:<contact>` flag; `signup` consumes it to stamp `emailVerified`/`phoneVerified` true (genuine state, not a client claim — DB-verified live). Additive, mobile/other clients unaffected. (3) 🔴 **`send-otp` existence gate** (user-reported) — it never checked registration, so an existing/logged-in user could OTP their own number; now `send-otp` rejects with **409 "account already exists, log in"** when the email (lowercased) or phone (matched across bare-10/+91/91/0 forms) already has a `User`. Safe across all callers (every `send-otp` use is new-account verification, none is login-by-OTP). (4) email-OTP **send** path still has a `sendEmail({to,subject,html})` vs `sendEmail(to,template,data)` signature mismatch — left (Email config-gated off; can't deliver regardless). **(C) Funnel QA sweep** (Playwright, logged in as seeded member): fixed 🟠 Dashboard "Today's Matches" === "Curated for You" (both ranked same pool identically → identical lists; now dedup Curated against daily + widen fetch, hide-if-empty), 🟠 MyProfileView lifestyle shown twice (DETAILS list + LIFESTYLE card → removed from DETAILS), 🟡 Login SSL/Privacy trust badges duplicated on desktop (`lg:hidden` the form-side copy), 🟡 "1 PHOTOS"→"1 Photo" plural. Home/Search/Subscription verified clean (the "0+/0K+" landing stats were a **CountUp scroll-spy artifact in static full-page screenshots, NOT a bug**). **magic-mcp learning:** emits **web React/Tailwind only (useless for RN)**; its generic profile-card results did **not** beat the purpose-built matrimony `ProfileCard` — used for *inspiration* (OTP boxes), adapted to brand tokens; did **not** bolt on components that would degrade the cohesive design. Regression: backend **135 unit** + FE **39** (added `smartContactField` + realigned `verificationStepEmail` tests) + builds green. Live-verified e2e: combined signup → verify → dashboard, account `emailVerified:true`; existing-contact OTP blocked inline. **New files:** `frontend/src/components/ui/OtpBoxes.jsx`, `frontend/src/components/onboarding/SmartContactField.jsx`. Dev-only OTP e2e used the codebase's `OTP_BYPASS_CODES` (set→tested→reverted; `.env` left as-found).

- 2026-07-02 **Auth/onboarding/hero conversion redesign + competitor-benchmark features (uncommitted).** Driven by `docs/10_Competitor_Visual_Benchmark_2026-07.md` (live Shaadi/Jeevansathi walkthroughs). **(A) Login rebuilt progressive/fintech-style** (`Login.jsx` full rewrite): identifier-first (`SmartContactField` reused — gained `id`/`label`/`hint` props, signup behavior unchanged) → Continue → password phase with "Signing in as +91 …" recap chip + Change; hidden `autocomplete=username` input keeps password managers working; AnimatePresence phase transitions + `layout` form (no jumps); lockout/Google/returnTo logic untouched. **Login-with-OTP deliberately NOT built** (user decision: OTP = verification only; `send-otp` 409s existing accounts by design). New i18n keys `auth.emailOrPhone/continue/change/signingInAs` ×3 langs. **(B) Onboarding polish (kept 2 steps):** signup-mode desktop rail swapped ring+stepper for light "Two steps. About two minutes." + slim segmented bar + 2 quiet step rows (guardian/edit modes keep full rail); mobile "Step 1 of 2"; final CTA "Create my profile"; DOB + name "why we ask" hints (`FormField hint`); step-2 description sharpened; OTP panel: manual Verify button removed (auto-verifies on last digit via `OtpBoxes onComplete`, `otpVerifying` spinner state, code cleared on error). **(C) Hero copy** (user picked from options): "From match / to mandap — / *all in the Tricity.*" + new subhead. **(D) Benchmark features:** `components/profile/PreferenceMatch.jsx` — reverse partner-preference checklist "Do you fit what X is looking for? 3/4" on ProfileDetail Looking-For tab (age/height/education/profession/city vs viewer's `me.Profile`; viewer-unfilled fields excluded from denominator, "add yours to compare"); `components/profile/PhotoGuide.jsx` — icon-tile DO/DON'T photo grid replacing text tips in `PhotosStep` (covers guardian onboarding + ModernProfileEditor); **post-signup profile-preview close card** in `ModernOnboarding` ("Your profile is live" + initials Avatar + "Name, age" + Just-joined badge → Complete my profile / dashboard; snapshots name/DOB into `previewData` BEFORE `clearDraft()` wipes formData). **Live-verified** (Playwright @375/768/1440): hero, login both phases + real member login e2e, full signup e2e w/ OTP bypass (set→reverted, test user deleted from DB), preview card → /profile/edit, PreferenceMatch 3/4 on seeded profile, PhotoGuide in editor. Regression: FE **39/39** + build green; backend untouched.

- 2026-07-02/03 **Photo/selfie verification + govt-ID removal (uncommitted).** User directive: we're not a govt authority — stop collecting IDs. **Backend:** `verificationController.submitVerification` rewritten selfie-only (requires multipart `selfiePhoto`; documentType/documentFront/Back **ignored, never stored** — legacy columns remain on model, no migration); `submitVerificationValidation` documentType now `.optional()` (stale clients tolerated); `getVerificationStatus` returns `selfiePhoto`; admin `getVerifications` now includes `User.attributes` allowlist + `Profile.photos` for side-by-side review; approve/reject notification copy → photo-verification wording. Verified-badge derivation unchanged (`Verification.status='approved'`, searchController). **Web member:** `Verification.jsx` reworked — Selfie-verification tier (camera-capture `SelfieField`, 3-step how-it-works, privacy note) + BG-check tier; doc-type dropdown/front/back gone; `Settings.jsx` VerificationTab same selfie-only rework (kept `FileUploadBox`). **Admin:** `AdminVerifications.jsx` rebuilt — "Photo Verifications" queue, per-card **Selfie vs Profile-photo cells**, review modal with large side-by-side + gallery strip + zoom lightbox; fixed 2 pre-existing bugs (name read `v.User.firstName` instead of `.User.Profile.*` → "undefined undefined"; `v.documentUrl` never existed → reviewers saw no images at all); `ImgWithFallback` hides dev's broken relative `/uploads/*` seed photos ("Image unavailable"); `AdminUserDetail` verification rows show selfie thumb instead of documentType. **Copy sweep:** all "Govt./Government ID" claims removed — Home (hero chip `100% photo-verified`, subhead, processSteps, FAQ, whyCards, badges-row, trust-cards), About ×2, Safety pillar → selfie-verification + human-review wording. **Live-verified e2e** (Playwright): priya member selfie upload → Cloudinary → status `Pending review` + trust 50% → admin queue card → review modal → Approve → approved tab + member status `approved` + "Profile Verified!" notification. Regression: BE **135/135** unit + FE **39/39** + build green. **RN impact:** RN ID-tier submit now 400s (selfie missing) — flagged in Known Issues, RN rework deferred.

- 2026-07-03/04 **Professional-minimalist refinement pass + deploy (via /autoplan pipeline — plan file `~/.gstack/projects/Saksham-20-tricitymatch/main-website-refinement-plan-20260703.md`, TODOS.md created).** Full review pipeline (CEO/design/eng subagent voices) then execution. **🔴 Real bugs found+fixed:** (1) **web profile editor never hydrated** — `ModernProfileEditor` mounted `OnboardingProvider mode="edit"` WITHOUT `existingProfile` (useState initializer needs it at mount) → every field blank for an 84%-complete profile; fetch lifted above the provider + selective merge onto the known form shape (raw API extras never leak into the submit payload; ISO DOB normalized). (2) **editor opened on step 3 (Religion) not Basic Info** — stale `Math.min(2,maxStep)` offset from when welcome/account steps occupied indexes 0-1. (3) **height/weight collectible NOWHERE** after the 2-step signup redesign (user-reported via prod screenshot) — completion meter scored them (+4 each) but no step had the inputs; added Height (4'6"–7'0" select, cm-stored) + Weight (kg) to `BasicInfoStep` in edit/guardian modes only (signup stays 2-field minimal); meter CTA copy "Add write about yourself"→field-level `cta`. (4) editor footer sat under mobile BottomNav (`pb-16 lg:pb-0`); edit mode no longer pollutes the signup localStorage draft. **Motion:** `utils/animations.js` rewritten 633→78 lines as the ONLY variant source (fadeRise ≤8px/280ms easeOut, fade, pageFade, staggerContainer 60ms, `staggerIndex(i)` capped `%6`, stepSlide directional; `fadeInUp` aliased for the 5 importers); `MotionConfig reducedMotion="user"` at App root (+ existing CSS block); MatchPopup confetti toned (50→18 particles, 3s→1.8s, kept as deliberate celebration — user decision), photo wiggle removed, avatar fallbacks → brand standard. **Dashboard:** error banner+retry (server failure no longer renders as zeros+"Complete Your Profile" — per-request catch-stubs removed), **first-run variant** (<60% completion or <48h account → 3-tile setup checklist replaces zero-stats), Who-Viewed lock hidden for free users until ≥1 real view, empty-state condition fixed (was ignoring dailyMatches/recentlyViewed) + made discovery-focused (no duplicate completion CTA), gold discipline (Today's-Matches header/chip/Total-Views de-golded; gold = premium only), "AI matched" chip→"For you" + family-voice greeting copy. **Search:** error card (retry) ≠ empty card (clear filters), 404 stays empty; pure-id card keys (index-coupled keys exit-animate wrong cards); stagger caps. **Washes killed** (pastel `primary→gold` gradients → flat `#FDF8F2`/`bg-primary-100`): Chat ×4, MatchCard avatar fallback + gold Message CTA→primary, auth pages, payment pages, Card `gradient` variant; Home "AI" copy → "matching engine/intelligent matching" (family voice). **Onboarding:** inline persistent submit-error + retry (toast alone missable), double-submit guard, directional step transitions, preview card deep-links `?section=photos` (editor supports `SECTION_INDEX`). Subscription: payments-opening-soon notice card when Razorpay unconfigured. **Test gates:** e2e login helpers rewritten two-phase (01/04/09 specs were broken-on-arrival vs progressive login; auth spec re-baselined green ×3 projects; `loginViaUI` in e2e/utils/helpers.js; playwright browsers installed) + 9 RTL tests (dashboardStates/searchStates — caught a real missing `FiAlertCircle` import) → FE **48/48**; BE 135/135. **Deterministic frontend Docker build:** standalone `frontend/package-lock.json` committed (Docker context can't see root workspace lockfile; .gitignore exception documented; regenerate via `npm install --package-lock-only` on a COPIED package.json — workspace refuses in-place) + Dockerfile `npm install`→`npm ci`. /design-review gate skipped by user (deploy priority). CLAUDE.md routing rules added.
- 2026-07-07 **Six-part refinement pass (via /autoplan — plan `~/.gstack/projects/Saksham-20-tricitymatch/main-refinement6-plan-20260707-170628.md`; codex outside-voice degraded on usage limit, single-model). (1) Background-check feature REMOVED** end-to-end: deleted the 4 `/verification/bg-check/*` routes + `utils/bgCheckService.js` + `env.js` bgCheck block + prod webhook-secret guard + `server.js` rawBody mount + `tests/unit/bgCheckWebhook.test.js`; web Verification "Background Check" tab gone (trust score now selfie-only 100%); RN `BackgroundCheckScreen` + nav route + `api/verification.ts` bg fns deleted. **Dormant `bgCheck*` DB columns LEFT in place** (no down-migration, matches govt-ID removal). Verified-badge derivation (Verification.status='approved') untouched. **(2) Selfie verification is now LIVE-CAMERA ONLY (web)** — new `components/verification/LiveSelfieCapture.jsx` (getUserMedia → `<video>` → canvas center-crop → JPEG File on the same `selfiePhoto` multipart field; mirror preview, capture unmirrored, iOS `playsInline`+`.play()`, track cleanup). **No file-upload path** anywhere in the selfie flow (user directive: uploads can be doctored). Camera-denied/absent → "use a device with a camera" message, no fallback. Wired into both `/verification` and Settings VerificationTab (old `FileUploadBox` deleted). RN selfie live-camera rework deferred (expo-camera native, config-gated). **(3) 🔴 Membership upgrade was impossible** — `subscriptionController.createOrder` threw `conflict('already have an active subscription')` for ANY active sub, so a paid member could never buy a higher tier. Fixed: TIER_RANK gate allows an order only when target tier > active tier (same/lower while active → clear 409); `verifyPayment` now supersedes the prior active sub (was only cancelling pending) so exactly one active sub remains + contact-unlocks reset to the new plan. Web `Subscription.jsx` CTA is tier-aware (Upgrade / Current Plan / Included). Fresh full term on upgrade (remaining days forfeited — accepted). **(4) ProfileDetail redesigned single-column** — the `lg:grid-cols-3` two-col layout (thin tab column beside a tall Details sidebar → big empty void, per user screenshot) collapsed to one centered `max-w-3xl` stack: mobile compat ring → full Details table (moved up) → tabs → contact → social. Kills the void; all data/actions/premium-gating preserved. (magic-mcp attempted for inspiration per user, returned a transport error → brand-native recreate.) **(5) New `/matches` page** (`pages/Matches.jsx`) — Saved (shortlist) / Mutual / Likes-You(premium, 403→upgrade state) tabs reusing `ProfileCard`, 4 states each; nav link "Matches" (FiHeart) added desktop+mobile with `navbar.matches` i18n ×3 langs. Closes "no way to see saved people." **(6) Transactional emails rebranded** — `utils/email.js` gained a shared table-based `brandLayout` (burgundy `#8B2346` header + gold `#C9A227` hairline + real support-email footer, no off-brand gradients); refactored welcome/passwordReset/matchNotification/subscriptionConfirmation/verificationApproved+Rejected + aligned weeklyDigest/otpCode/securityAlert. Subscription email: killed the green gradient, friendly plan label (`basic_premium`→"Basic Premium"), formatted valid-until, CTA fixed `/browse`(dead)→`/dashboard`. No test coupled to template strings. Regression: **BE 155/155 unit · FE build ✓ · mobile tsc 0**. Not live-driven (camera=hardware, upgrade=Razorpay unconfigured in dev); build+lint+type verified. No commit.

- 2026-08-16 **RN full workflow review — production readiness + real-device install (planned on Fable, implemented on Opus; plan `~/.claude/plans/gentle-whistling-yao.md`).** First post-merge walkthrough since `26f1ca5`: RN-A/B had only ever verified Welcome+Login on device, so **every authed screen was unsmoked since the SDK-52 rebuild**. Built and drove both platforms live (API 35 emulator via `adb input`+`uiautomator`, iPhone 17 Pro sim via `idb` — **idb wants POINTS, screenshots are PIXELS, divide by 3**). Commits `49c6564` → `f6c914e` → `ec74842` → `71cb07d` → `6b5e485` → `c1ec184` → `447b020` → `0498899` → `5f9cd3a` → `a6b0dd0` on `main`, **not pushed, not deployed**. **(1) Safe areas** — every authed screen compensated for the status bar with a hardcoded constant (Home `paddingTop:56`, ProfileDetail `top:48`, OwnProfile 52, auth 48/60) and **three inset strategies coexisted**; targetSdk 35 forces edge-to-edge so the constants are no longer even approximately right. Unified on `useSafeAreaInsets`; gave the sticky bottom bars (ProfileDetail actions, chat composer, Subscription/Quiz footers) a real bottom inset — they were sitting under the gesture pill; killed Subscription's double top pad; LegalLayout off the deprecated RN `SafeAreaView`. **(2) 🔴 Fabricated payment results** — `openRazorpay` caught *everything* and returned `pay_DEV_STUB`/`DEV_STUB_SIG` with **no `__DEV__` guard**, so (a) a missing native module fed a fake payment into `/verify-payment` in release, and (b) a user who **cancelled a real checkout** produced the same fake success. Module load split from the payment call; `PaymentsUnavailableError` in release; real outcomes propagate. `CONFIG.IS_RAZORPAY_CONFIGURED` had **zero consumers** → the Android chooser and the footer both advertised Razorpay with a placeholder key. **(3) Invented data removed** — the astrologer marketplace substituted **four invented practitioners** (names/ratings/review counts/prices) whenever the API returned `[]`, which it always does; the detail screen hardcoded a 4.8/342-review rating, six slots, three named reviewers and a **certification claim** behind a Book CTA whose endpoint doesn't exist. **(4) Unearnable verification UI** — OwnProfile still rendered the removed 4-rung ladder (Mobile/ID/Education/Income) with `idVerified={false}` **hardcoded at the call site**, so 3 of 4 badges could never be earned and "Get Verified" showed to verified members; VerificationScreen's mobile row was a **hardcoded green "Verified"** pill regardless of `phoneVerified` (the QA account has it false). Both now read real state. Also: Settings promised "Notification Preferences" and opened the inbox (no prefs backend); Step14 congratulated everyone on an invented **72%**; elder mode removes the Chat tab so Matches' chat CTAs were **silent no-ops**; ChatThread opened **titleless** from a notification tap (`name: ''`); brand drift (TricityShadi in legal ×4 + Settings + one Razorpay merchant name); deleted the unreachable OTP route, its unused OtpInput, and the 0-consumer upgrade-modal store slice. **(5) 🔴 Store-blocking permissions** — the **release APK** requested `SYSTEM_ALERT_WINDOW`, `WRITE_EXTERNAL_STORAGE` and `READ_PHONE_STATE` (the last merged in by `com.razorpay:core:1.0.1`, a Play **restricted** permission). STORE_READINESS claimed the first two were already removed; they never were, and they survive `prebuild --clean` because they are **merged in, not stale files**. Blocked via `android.blockedPermissions`, then **verified in the artifact** (`aapt dump permissions app-release.apk`) — the first attempt silently didn't apply because app.json changed *after* the last prebuild. **(6) Docs corrected** — `STORE_LAUNCH_CHECKLIST.md` told you to create **`tricityshadi_*` Play products** (real IDs are `tricitymatch_*`, and **Play product IDs are permanent**); targetSdk 35 was ticked on a commit message from a commit that had never compiled; iap/localization called "not installed" (they are) while **Agora + Firebase messaging — genuinely absent from package.json — were described as config-gated**. READINESS.md rewritten. **(7) Onboarding funnel** (`447b020`, never driven since the SDK-52 rebuild) — **30 i18n keys were referenced but undefined**: `t('k','English default')` degraded silently (hi/pa quietly showed English) while `t('k')` printed the raw key, so Step 12 (photos) and **Step 13 — the OTP gate** rendered `onboarding.step13.otpTitle` etc. as literal text on screen; added all 30 ×3 langs, dropped 5 dead ones, `step13.enterOtp` re-worded 6-digit→**4-digit** to match MSG91. Step 14's ring read **0% for a 64%-complete profile**: `Animated.Text` with an interpolated node as *children* renders once and freezes in RN 0.76 — the pre-existing hardcoded 72% was frozen the same way, which is why nobody caught it; text now comes from state, `Animated` drives the arc only. My own regression caught in the same pass: a `getMe()`+`setUser()` on Step 14 mount flipped the server-derived `onboardingComplete` true, so RootNavigator swapped Onboarding→Main and the celebration screen **dismissed itself** — read the figure, don't commit the user mid-flow. Height picker emitted one row per **cm** with an inch-rounded label so `4'0"` appeared three times across 93 entries → one row per inch, 4'6"–7'0", matching web; `step1.weight` no longer renders "(Optional)" twice. **(8) Astrologer over-removal corrected** (`0498899`) — pass 1 had gutted `AstrologerDetailScreen` to a "booking not open" notice on the strength of a **stale in-code comment**; `GET /astrologers/:id` and `POST /astrologers/book` both work (verified with curl — booking returns a real Razorpay order) and the dev DB **is** seeded with Astrologer rows, contradicting the CLAUDE.md note. Screen rebuilt data-driven with loading/error states and real pricing. Two root causes behind the invented UI: `bookAstrologer` returned `res.data` for a `{success,booking}` envelope, and **`getAstrologer(id)` didn't exist at all** — missing client function → invented screen is a recurring shape here. Probe the endpoint before deleting a feature. **(9) Admin stack** (`5f9cd3a`, never driven before) — all three admin GETs had the **envelope bug**, and `AdminStats` was an invented shape (`signupsToday`/`activeSubscriptions`/`revenueToday` are names the server never sent) → blank tiles and a literal **`₹undefined`**; realigned to server truth (`totalUsers/verifiedUsers/activeSubscribers/revenueThisMonth`) with defensive `?? 0`. `ReportsQueueScreen` declared `category`/`reporterName`/`reportedName` while the server sends `reason` + nested `Reporter`/`ReportedUser`, so `item.category.replace()` **took the screen down the moment one report existed** — invisible while the dev queue was empty (seed one row of every moderation type before trusting a queue screen). And **`SafeAreaView` imported from `react-native` is a no-op on Android** (iOS-only): 13 files did it, so the Admin Console title was drawn over the status-bar clock — all moved to `react-native-safe-area-context`. **(10) Verification queue** (`a6b0dd0`) — the RN queue was still document/tier-based, months after the product went **selfie-only**, and showed no images at all; rewritten as a real side-by-side **selfie vs profile-photo** comparison (the thing an approver actually needs) via `SmartImage`, `DOC_LABELS`/tier badges deleted. **Bureau deliberately left alone:** `enum_Users_role` has no `bureau` value and no `/bureau` routes exist, while `shared/src/types/user.ts` includes it — TS lies, the stack is unreachable by construction. Already pinned as known-dead in `docs/BUREAU_CHANNEL_MEMO_2026-08-09.md` + `apiConformance.test.ts`; **deleting it is an owner call**. **Verified live both platforms:** login → Home → Search + Filters sheet (gorhom v5 fine) → Matches ×3 tabs → Chat list/thread/**send** → Profile → Settings → Verification → Subscription → ProfileDetail; Android additionally walked the fresh-signup **onboarding funnel** (Steps 0–14, OTP send skipped — MSG91 is live), the **admin stack** (console tiles, Verification queue, Reports queue), cold-start session restore, hi/pa switching, and cross-device chat sync (a message sent on Android read back on the iOS sim). A later iOS pass closed the admin half (`f76e36d`, `360e205`): logout → admin login → console (55 users / ₹13,498 / 34 verified, real figures) → Reports queue with the one seeded report → Verification queue driven **with data** (a pending row seeded with reachable selfie + profile-photo URLs) and **Approve round-tripped to `status='approved'`**, queue emptying. Two bugs that only appeared there: the reports card's fixed 60pt label column broke "Reported:" onto two lines on iOS, and **the Admin Console was a dead end** — it is the root of its own nested stack, so it drew no back button and the iOS swipe-back gesture never reached the parent navigator (Android's hardware back had masked it); an admin was stuck until force-quit. A final pass closed the last gap — the **iOS onboarding funnel, Steps 0→14, driven live** on a fresh signup (test account soft-deleted after via `DELETE /auth/account`). It found the funnel's own blocker: 🔴 **`4b3ccd5` — DOB was untypable on iOS.** Step 1 asks for DD/MM/YYYY behind `keyboardType="numeric"` and only accepted the value once it contained two slashes, but that keypad has no `/` key — a real user could enter the eight digits and still leave Save & Continue disabled at the **first step of onboarding**. (Earlier Android passes missed it because `adb input text` injects `/` directly, bypassing the keyboard.) Separators are now inserted as they type; the wedding-date field in Share Your Story had the same shape and got the same fix. Everything else held: step 12/13 render real copy (the 30-key i18n fix), the height picker lists unique inch rows, "Weight (Optional)" appears once, and **Step 14 showed a real 78% and did not self-dismiss**. Note the 🇮🇳 flag rendering as tofu at the OTP gate is a **simulator artifact** (the iOS Simulator ships no flag glyphs) — investigated and reverted, not a bug. **Method notes: a Metro reload resets a debug build to the initial route** — a "screen didn't open" can be that, not a bug; **React Query's cache survives Fast Refresh**, so a redbox right after changing an unwrap may be the *old* shape still cached — force-stop and relaunch before believing it. Gates: **BE 259 · FE 97 · mobile 57 · mobile tsc 0 · root lint 0 errors · slop-lint clean**; `targetSdk=35` confirmed via `adb shell dumpsys package`. **Still open (owner):** Apple Developer account, Play Console (12-tester/14-day clock), `eas init` (`projectId` is still the literal `tricityshadi-app` so **no EAS build can run**), live Razorpay keys, Sentry DSN. **Still open (code):** iOS associated-domains + `assetlinks.json` for https deep links (custom scheme works), RN selfie live-camera rework, dark-mode retrofit.

- 2026-08-19 **Modernization phases A–G COMPLETE (via /autoplan plan `good-now-create-a-sunny-mitten.md`; commits `…`→`ccacbc7` + CLAUDE.md, PUSHED + web deployed per phase).** Full P0 + UX plan (D1–D8 + DS1–DS11 + ES1–ES12 + 4 owner premise-gate decisions) shipped. **Backend (A, deployed):** migrations **000049–51** (ChatGrants / Message rich fields messageType-mediaUrl-replyToId-reactions / Match note+likedItem snapshot) applied dev+prod; free-reply window D1 (grant = freeUserId-only, READ forever / SEND 5 msgs·48h, transactional row-lock — PG race probe PASS) behind **`FREE_REPLY_WINDOW` default FALSE in prod** (owner flips at ~≥10 mutual/wk); server-authoritative sockets (REST dual-emits legacy + `message:new/edited/deleted/reaction`; legacy client relays no-opped same commit — dual-emit removal TODO next release); reactions (6-emoji allowlist, row-lock toggle) / voice messages (Cloudinary video type, 5MB, uploadLimiter, paid) / reply-quote; `GET /match/sent`; deriveReasons chips on sliced daily set (`daily-matches:v2`); saved-searches CRUD (lifestylePreferences JSONB, cap 5); **biodata PDF** `GET /profile/me/biodata?template=classic|modern` (FREE, photo f_jpg 2s-timeout, "Made with TricityMatch" footer); **`ASTROLOGER_MARKETPLACE` flag default false = astrologer routes 404** (kill-switch, clients hide via `user.features`). **Web (B+E, deployed):** Chat.jsx rewritten (namespaced listeners + dedupe-by-id, reply-quote, reactions, VoiceRecorder webm/opus, DS1 scripted paywall composer exhausted/expired variants, DS3 reply meter, locked rows ES5); like-with-note modal (DS5 shows liked thing); Matches Sent tab; SuggestionCard reason chips; SavedSearches UI; BiodataCard flagship flow (DS9 template picker→generate→WhatsApp/download); subscription long-scroll; **Skeleton/EmptyState/ErrorState primitives + adoption** (Dashboard/Search/Matches/PaymentHistory) + `.dark .skeleton` gradient; **StagedLoader DS6** (once/day sessionStorage, skippable, reduced-motion static, error kills theater; Dashboard hold ≤1.5s, Search maxHold 0); **View Transitions** on nav links (`viewTransition` + @supports CSS); Home progress bar CSS `animation-timeline: scroll()` w/ framer fallback. **Mobile (C+D+E+F, pushed — NO device smoke yet):** bureau stack DELETED (features/api/shared type); useSocket event contract fixed; chat parity (replyWindow composer, reactions long-press, voice via expo-av m4a, reply swipe); Sent tab; SaveSearchModal wired; biodata share (lazy expo-file-system); **D6 onboarding rebuilt** — Signup route = `CreateAccountScreen` (SmartContactInput email/phone + inline 4-digit OTP, create-after-verify) → `BasicsScreen` (profile-for chips, names, gender, DOB slash auto-insert) → signup carries basics → server derives onboardingComplete → straight to Main; Step0/1/13/14 + SignupScreen DELETED; OnboardingNavigator = single `CompleteBasicsScreen` legacy gate; **Steps 2–12 re-registered as skippable journey modal group in MainNavigator** under parameterized OnboardingProvider (injected nested nav `navigate('Main',{screen})`, `start()` resumes at first incomplete step, no navigate-on-mount) + `JourneyFinaleScreen` (DS2 ≥4-results reveal guard else honest early-state, DS6 staged copy, DS7 single gold tease, Quiz CTA); HomeScreen auto-presents once w/ 7-day AsyncStorage re-prompt; **E+F makeStyles(c) sweep: 88 files converted** via codemod (`(c: ThemeColours) => StyleSheet.create` factories + inline swaps + config-map factories; shared `ThemeColours` type added), 0 module-scope light palettes remain → **`DEFAULT_DARK_MODE_OVERRIDE` flipped false→null (dark follows system)**. **Gates (G):** BE **262** unit · FE **108** · mobile **57** + tsc 0 · eslint 0 errors ×3 · builds ✓ · prod smoke ✓ (health, features all false, biodata %PDF, daily v2 reasons live, legacy tricityshadi /api 200, socket.io 200). **Owner items:** flip FREE_REPLY_WINDOW / ASTROLOGER_MARKETPLACE when ready; Razorpay live keys; Sentry DSN. **RN Android sim smoke DONE 2026-08-19** — door e2e (409 gate, +91 pill, OTP auto-verify, DOB slash, signup→Main direct), journey auto-present + X-exit + force-kill resume-at-first-incomplete, cold-start restore, **system dark mode live-switch verified across Home/Search/Matches/Chat-gate/Profile/Settings/Subscription/journey/auth** (all correct; 1 copy fix `cdceefa`). Smoke method: SMS_PROVIDER=dev + OTP_BYPASS_CODES set→REVERTED, smoke account deleted. **iOS sim smoke still owed before store build.**
- 2026-08-19 **Growth/UX ideas batch — owner's 3 + 6 researched additions, BOTH platforms (commits `e2d4a30`/`2e8c0b0`/`b1fa368`, PUSHED + web/backend deployed prod).** Owner ideas: (1) journey optimization, (2) floating bottom nav, (3) dashboard empty-space fill; my additions from the research pass: new-this-week counter, finale→biodata WhatsApp hook, post-first-like notification priming, Step-10 prompts, voice-intro nudge, midnight-refresh line. **Backend:** `GET /api/v1/stats/community` (auth; `{newThisWeek,totalMembers}`; active+visible profiles only; Redis 1h key `stats:community:v1`; manifest-pinned). **RN:** journey chapters — "Step N of 12" → 4 named chapters (roots/life/world/match in `JOURNEY_CHAPTERS`, `chapterForStep()`), endowed-progress bar (starts 20%), chapter dots + entry celebration pill in `OnboardingLayout`; conditional reveals (sub-caste/gotra⇐caste, birth details⇐manglik, degree/institution⇐education, employer/income⇐profession); Step10 optional prompts (≤2, `constants/prompts.ts` flat JSONB mapping); `FloatingTabBar` pill (translucent `surfaceCard+'F2'`, keyboard-hide, haptics; `TAB_BAR_CLEARANCE=92` via `useTabBarClearance()` on 5 tab-root scrollers; **elder mode keeps docked default bar**); `DiscoverCards` stage-aware 2-card fill (early: verify/invite/voice; established ≥60%: gold membership founding-aware + success story) + community strip + midnight line on Home; `useBiodataShare` hook extracted → finale biodata CTA; notification priming (`utils/notifPrime` AsyncStorage accepted/declined, `NotificationPrimingSheet` in MainNavigator, fires on first like from Search/ProfileDetail, `useNotificationHandler(userId, enabled)` gated — FCM registers only after accept); i18n ×3 langs. **Web mirrors:** BottomNav → floating pill (same occupied height, dark-aware); Dashboard community chip + midnight line; post-signup preview card biodata CTA → /profile; onboarding conditional reveals on Religion/Horoscope/Education steps (hidden fields keep saved values). **🔴 Pre-existing prod bug found by lint gate + fixed:** Settings `AccountTab` referenced `user` without `useAuth()` (phase-B astrologer-flag edit) → ReferenceError crashed the Account tab in prod since the phase-B deploy. Gates: BE **277** · FE **108** + build · mobile **57** + tsc 0 · eslint 0 errors. Prod smoke: health OK, `/stats/community` live (`{newThisWeek:1,totalMembers:12}`), site 200. **Android sim smoke of the batch DONE same day** — chapters header/dots/endowed pill render, caste→sub-caste/gotra reveal live, floating pill bar (taps + visual, content clears it), Home community strip + midnight line + DiscoverCards (story+verify for VIP; verify→Verification nav), priming sheet fired on FIRST like → accept stored `notifPrime=accepted` + FCM cleanly skipped, cold-start restore. **2 more real bugs found+fixed in the smoke (`64414db`):** 🔴 Search screen 100% touch-dead — gorhom v5 keeps the CLOSED FilterPanel's `BottomSheetBackdrop` mounted full-screen clickable (a11y dump: Button "Bottom sheet backdrop" [0,136][1080,2337]) swallowing every tap on list/filters/search-input; pre-existing (repro'd on `cdceefa`) → backdrop now rendered only while the sheet is open (`onChange` state) + `enableDynamicSizing={false}`; verified live like→row, sheet open/close cycle. 🟠 FCM `require('@react-native-firebase/messaging')` (uninstalled pkg) redboxed through try/catch on priming accept (haptics R3-1 class) → probe `NativeModules.RNFBMessagingModule` before requiring. Still owed: biodata finale CTA + Step10 prompts + hi/pa render of new keys (with the standing iOS sim smoke).
- 2026-08-18 **Final launch walkthrough + repo clean (commits `0aa1433`→`ec7b3c1`, PUSHED).** Full gates green (BE 259 · FE 97 · mobile 57 · tsc 0 · lint clean · build ✓). Repo tracking cleaned: stale root plans/audits, `graphify-out/` (232 files), 11MB competitor shots, `.cursor`, root `app.json`/`API_DOCUMENTATION.md` untracked; agent tooling/brag outputs gitignored; 264MB local qa-reports deleted. Launch fixes: public **`/delete-account`** page (Play data-safety URL; steps match Settings "Danger Zone"), Privacy DPDP additions (breach notification, nominate right; web + RN mirror, Aug 2026), compose **log-rotation on all services**, RN release **console strip** (babel `transform-remove-console`, keeps error/warn). Deployed to prod (frontend rebuild + db/redis recreated with logging; verified live: /delete-account 200, health OK, sitemap updated). VPS: **daily pg_dump backup cron added** (`/root/tricitymatch-db-backup.sh`, 22:30 UTC, 14-day retention, first dump verified); `.env.production` already 600. SSH hardening prepared but **declined by user** — left as owner item. Checklist = **`docs/LAUNCH_CHECKLIST_2026-08-18.md`** (single source of truth; remaining items are ALL owner credentials: Razorpay live keys, eas init projectId, Apple/Play accounts, mailboxes + grievance-officer name, Sentry/FCM, Hostinger off-site backup verify).

- 2026-08-20 **New TM logo rolled out everywhere + custom cursor removed + tricityshadi→tricitymatch sweep (commit `0e8de58`, PUSHED + web/backend DEPLOYED to prod).** Source of truth = `~/Desktop/Graphics/final TM .svg` (1000x1000, white serif TM on **#B30021**, glyphs centred on (500,500), bbox 110.4..889.6 / 251.64..748.36). **(1) `scripts/generate-brand-assets.mjs` re-sourced** off the new artwork (polygons, never `<text>` — an SVG in an `<img>` can't see page webfonts) and **extended to cover two asset families it never emitted**: the 40 `apple-splash-*.jpg` and the committed native projects. Cuts: LOGO (as drawn; callers round their own corners so `Logo.jsx`/`Logo.tsx` needed **no change**), FAVICON (glyphs pushed to 90% width — the artwork's default 78% is mud at 16px), MASKABLE (safe circle r=0.4), ADAPTIVE (centre 66%), SPLASH (glyphs on transparency), ROUND (Android `ic_launcher_round` disc), plus the OG card (field stays UI burgundy; logo sits on a **white** tile because red-on-burgundy has no contrast). **🔴 Found: the 40 `apple-splash-*.jpg` still carried the old "TS" TricityShadi monogram** — never regenerated in the July rebrand, and **referenced by nothing** (no `apple-touch-startup-image` link, no manifest entry) so they ship as dead weight. Regenerated in-brand rather than deleted (wiring or dropping them is an owner call). **(2) Native icons written directly** into `mobile/ios/.../AppIcon.appiconset` (flattened — App Store rejects alpha on the marketing icon), `SplashScreenLogo.imageset` ×3, `SplashScreenBackground.colorset`, and all 5 Android dpi buckets (`ic_launcher`/`_round`/`_foreground`.webp — note Expo writes **PNG bytes under a .webp extension**; Android reads by content — plus `splashscreen_logo.png`). Done WITHOUT `expo prebuild`, which would churn native config right before a store build; a later prebuild regenerates the same result. Brand colours: `values/colors.xml` splashscreen_background #8B2346→#B30021 + iconBackground #B60D2F→#B30021; `app.json` both backgroundColors →#B30021. **The UI brand burgundy #8B2346 is UNCHANGED** — #B30021 is the mark's own colour, not a palette swap. **(2b) Tab icon / favicon cache-bust** — after the swap the browser tab still drew the OLD burgundy mark while the page logo was already new. The bytes were correct (`favicon.svg` + both PNGs all `#B30021`, dist included): **Chrome stores favicons in a cache separate from the HTTP cache, so a rebranded icon at an unchanged URL survives a hard reload.** Fixed durably by versioning the URLs — `?v=2` on the svg/32/16/apple-touch links and on `manifest.json` + all 8 manifest icon `src`s (so installed PWAs re-fetch too), with a comment saying to bump `v` on every future mark change. `msapplication-TileColor` #8B2346→#B30021 (it backs the icon tile); **`theme-color` stays UI burgundy** (browser chrome, not the mark). **(3) Custom cursor removed** — `Home.jsx` set `body { cursor: none }` and drew a dot+ring follower; the CSS block, the `Cursor` component, its reduced-motion overrides and the mount are gone, `overflow-x: hidden` kept. Verified live: `getComputedStyle(document.body).cursor === 'auto'`, 0 `.cur-*` nodes. **(4) tricityshadi→tricitymatch sweep.** Every user-facing support address in `frontend/src` + `mobile/src` was **already** `@tricitymatch.com`; the stale ones were backend defaults, env templates, compose, CI, monitoring and docs. Changed: `config/env.js` (EMAIL_FROM/REPLY_TO/SUPPORT/ADMIN defaults), `swagger.js`, `email.js` + `security.js` comments, `.env.example`, `.env.production.example`, `.env.development` (**which still had `EMAIL_FROM_NAME=TricityShadi` — dev mail was signing as the old brand**), `docker-compose.yml`, `.github/workflows/deploy.yml` environment URLs, `monitoring/alertmanager.yml`, `nginx/ssl/README.md` (+ aligned its cert filenames to the `fullchain.pem`/`privkey.pem` that `nginx.conf` actually uses — pre-existing drift), README, and the live docs (QA/API-ref/tester-list/DEPLOYMENT/architecture/HOSTINGER + the `tricityshadi://` deep-link scheme in 05_Feature_Tickets, which is really `tricitymatch://`). Social links → `/tricitymatch` (owner decision; **they 404 until the handles are actually renamed**). `shared/package.json` `@tricityshadi/shared`→`@tricitymatch/shared` — safe because nothing imports it by specifier (everything uses the `@shared/*` alias); lock diff is exactly 2 lines. **Deliberately KEPT** (identity, not display): bundle IDs `com.tricityshadi.app` (+ the Java package dir, Gradle, Info.plist, pbxproj), EAS slug, the MMKV store id `tricityshadi`, `GOOGLE_PLAY_PACKAGE_NAME` (now carries a comment saying it is not a typo — a published package name is permanent), the legacy `tricityshadi.com` nginx proxy that shipped mobile builds still call, `scripts/rebrand-stored-data.sql`, and the dated audit reports. **Gates: BE 262 unit · FE 108 · mobile tsc 0 · frontend build ✓ · eslint 0 errors.** `npm run lint` still **exits 1 on a PRE-EXISTING slop-lint failure** (4 off-token hexes in `BiodataCard.jsx` + `toastConfig.tsx`, files untouched here — confirmed red on stashed `main` too). Live-verified on the web app: new mark in navbar + login rail, favicon swapped, cursor normal, footer socials updated. **(5) Email-domain cutover started same day.** `tricitymatch.com` created in Resend (`ba81441a-e368-4cd5-b6f3-3c10b88eca8c`, ap-northeast-1) and its Hostinger DNS fixed, both over REST. **Two records had been hand-copied from tricityshadi and were both wrong:** `send` TXT held the **MX value pasted into a TXT** (so there was no SPF at all), and `resend._domainkey` was a byte-identical copy of tricityshadi's key — **Resend issues a per-domain DKIM keypair, so a copied key can never verify** (the real one starts `...BgQD2eqc61YGTphnc...` vs the copied `...BgQCwgOIvxOELks...`). SPF TXT + SPF MX + Receiving MX **verified**; DKIM correct authoritatively, pending only on an 8.8.8.8 cache my own earlier `dig` probes populated at the old 14400s TTL. Method: Hostinger `GET/PUT /api/dns/v1/zones/{domain}`; **`overwrite` semantics are undocumented and there is no public OpenAPI spec**, so every PUT carried the COMPLETE zone (merge and replace then converge) with a snapshot + auto-rollback — verified 6→7 record sets, A record never at risk. TXT `content` stores **with literal quotes**; MX embeds priority + trailing dot. **Cloudflare fronts api.resend.com and 1010-bans urllib's default User-Agent** — set a real UA or use curl. **Prod deploy did NOT touch email**: `/var/www/tricitymatch/.env` explicitly pins `EMAIL_FROM=noreply@tricityshadi.com`, so the verified sender kept working; confirmed in the recreated container. **Note `.env.production` on the VPS is a stale 910-byte May-4 file that compose IGNORES** (no `env_file:` directive) — the real file is `.env`. Follow-up plan: `~/.claude/plans/wobbly-sprouting-rainbow.md`. **Owner items: once Resend shows verified, flip EMAIL_FROM/REPLY_TO/SUPPORT in `/var/www/tricitymatch/.env` (SUPPORT_EMAIL is user-visible — it renders in every email footer) and recreate backend only; rotate the Hostinger API token pasted in chat; rename the Instagram/Facebook/Twitter handles (footer already points at /tricitymatch and 404s until renamed); a readable inbox for support@/grievance@ still needs inbound routing or a mail host (Resend inbound is a webhook, not a mailbox); RN app icon needs a rebuild to show on device.**

- 2026-08-20 **Full production security audit + remediation, then final pre-launch QA (branch `security/prod-hardening-2026-08-20`, 13 commits `5aeae20`→`4a38fb4`, NOT merged, NOT pushed, NOT deployed).** Authorized inspect→attack→fix→retest pass; plan `~/.claude/plans/final-production-security-parallel-dawn.md` (34 findings: 7 High, 10 Medium, 17 Low). Posture going in was already strong — helmet configured, CORS allowlisted, 16 limiters, magic-byte upload checks, timing-safe webhook HMAC, refresh rotation + family revoke, `PROFILE_EDITABLE_FIELDS` blocking mass-assignment; **no SQLi, no hardcoded secrets in git, no admin BFLA.** Risk was concentrated in missed authz gates and prod escape hatches. **Highs, each reproduced before fixing:** (H-1) `GET /calls/agora-token` minted an Agora **join credential for ANY channel string** with `uid=0` — `initiate`'s mutual-match gate was bypassable by asking for the channel directly, and astrologer channels are the guessable `ast_<bookingId>`; now asserts CallSession participation or booking ownership. (H-2) **account lockout was completely inert** — `checkAccountLockout` read `req.body.email` but every modern client posts `identifier`, so no key → `next()` → the `lockout:` counters were written and never read; phone logins were unlockable by construction. Shared `loginLookupKey` now used on both check and record. (H-3) **IDOR on compatibility / horoscope-match / kundli-PDF** — three `/:userId/...` routes were `auth`-only and skipped every gate `getProfile` enforces; the PDF renders full name, exact DOB and place of birth, so it was an **enumeration oracle over the whole user table** including banned, deleted and `matches_only` accounts. Proven by `git stash`ing the fix and pulling a real 2,389-byte PDF for a `matches_only` stranger (200 → 403 after). Extracted `assertProfileVisible()`; also added the **missing Block check to `getProfile` itself**. (H-4) `OTP_BYPASS_CODES` (master OTP = universal account takeover) and `ALLOW_INSECURE_PROD` were shipped, compose-plumbed and warn-only — **and the warning lived inside the `ALLOW_INSECURE_PROD` branch, so shipping master OTP codes with that flag off printed nothing at all**; both now fatal at boot. (H-5) weak-secret guard only rejected literal `root`, so the compose default `postgres` passed, and `hasRealSecret()` was applied to Razorpay but **not** to JWT/COOKIE/DB; compose `:-postgres` → `:?`. (H-6/H-7, latent behind flags) astrologer verify never bound `razorpay_order_id` to the booking nor checked payment-id reuse (one genuine triple replayed across every pending booking) and fail-**opened** to `confirmed` on any order-creation error; Google Play idempotency was keyed `(userId, purchaseToken)` not the token alone, so one real token activated top tier on unbounded accounts — migration **000052** adds partial unique indexes on both `razorpayPaymentId` columns (fails loudly on pre-existing dupes). **Mediums/Lows:** socket auth never checked account status (a correct `socketAuth` existed at `middlewares/auth.js` as **dead unimported code**); enumeration oracles on signup/send-otp/login-Google-branch (`signupLimiter skipFailedRequests:true` made 409 probes free); `unlock-contact` burned paid quota on unvalidated targets; `/monitoring/health/full` unauthenticated, outside the limiter, publicly proxied, doing real DB+Redis round-trips per request; `Secure` cookie flag conditional on `FRONTEND_URL` scheme; **no log redaction** (secret-bearing `/invite/:token` paths went to app + nginx logs); `savedSearches` writable raw through `PUT /profile/me`, bypassing the sanitizer and cap, then consumed by the Bull digest worker; refund ignored consumed unlocks; search `interestTags` object → 500. **A verified CSP now ships on the static SPA** — the previous draft was commented out AND wrong (no `font-src`, missing Razorpay hosts); enabling it required moving SW registration out of an inline `<script>` so `script-src` stays free of `unsafe-inline`. Confirmed in a real browser: 11 font faces across 5 families, zero console errors. **False positives corrected, reported NOT FOUND:** the "ungated prod `console.log`" calls were all already `isDevelopment`-gated, and multer 1.x has **no current advisory** (`npm audit` clean) so it was left alone rather than blind-upgraded. **Method notes:** `asyncHandler` is `Promise.resolve(fn()).catch(next)` — it does **not** return the promise, so test assertions race the chain (`await new Promise(r => setImmediate(r))`); regenerating the root lockfile on macOS **drops Linux `@sentry/cli` binaries** and breaks CI `npm ci` (use targeted `npm install --package-lock-only`); `frontend/package.json` had drifted from its standalone Docker lockfile. My own regression, caught by a test I added: `assertProfileVisible` 403'd members viewing their **own** `matches_only` profile, because `getProfile` short-circuits self-view and the extracted helper didn't. **Then the final live QA pass** (report `docs/QA_FINAL_2026-08-20.md`, 33 screenshots in untracked `qa-final-2026-08-20/`): web + Android emulator + iOS sim, focused on the modernization and growth batches. **2 real bugs found + fixed:** Search's filter sheet **stayed open across tab switches** (tab navigator keeps the screen mounted and gorhom holds its own position → every later visit opened with filters covering the results; `useFocusEffect` blur-close), and the **floating pill tab bar drew on top of that open sheet**, covering both "Save search" and "Show N profiles" — the sheet's only apply path — on **both** platforms; fixed with an ephemeral `bottomSheetOpen` flag in `uiStore` that hides the bar for a sheet the way it already hid for the keyboard (elder mode unaffected, it uses the docked bar). Caught from **screenshots, not the a11y tree** — both controls are present in the tree, just covered. Web was already correct (`z-[60]` sheet over `z-50` nav). **Documented, not fixed:** Android biodata "Share PDF" ships only the caption because RN `Share.share()` honours `url` on **iOS only**; the `expo-sharing` fix was attempted and reverted — npm hoists it to the workspace root so the rebuilt APK got its FileProvider resources but **not** the JS module registration (`require` resolved `undefined`), and `npm uninstall` then pruned shared workspace deps and redboxed the app. **Run down and cleared as NOT bugs:** "9999 unlocks remaining" is a hand-seeded VIP row (real unlimited sentinel is `NULL` → `-1` → no count rendered), and "5000 new profiles this week" is truthful (dev corpus has 5,002 users created in 7 days). **Method note: a debug build keeps serving a stale JS bundle** — the first iOS verification of the sheet fix "failed" until force-quit + relaunch. **Gates: BE 333 unit · FE 108 · mobile 57 + tsc 0 · eslint 0 errors · FE build ✓.** **MERGED to `main`, PUSHED and DEPLOYED to prod 2026-08-20** (merge `0a6e3f7`, follow-up `431e211`). Deploy notes: pre-flight confirmed prod `.env` trips none of the new fatal guards and the DB held **0 duplicate `razorpayPaymentId` rows**, so migration **000052 applied cleanly on boot** (umzug) — both partial unique indexes verified in `pg_indexes`. **The CSP was already live in production all along, on the HOST nginx site (`/etc/nginx/sites-available/tricitymatch.com`), NOT the compose `nginx` service — prod does not run that container**, so `nginx/conf.d/security-headers.conf` is dead config in this topology; the host CSP pins the SW-registration inline script by `sha256-ap57…`, and since this branch moved that script into the bundle the hash now matches nothing (harmless, and `script-src` no longer needs it — verified the shipped `index.html` has exactly one inline script left, the non-executable JSON-LD). Backend/frontend host ports are now **127.0.0.1-only** (3002/5002), which is safe because the host nginx already proxied to `127.0.0.1`; confirmed externally closed while co-tenants (edumapping, school.globoniks, tricitylifeinsurance) stayed 200. **One regression I introduced and fixed in the same session:** M-9's header re-declaration surfaced a conflict — the SPA container sent `X-Frame-Options: SAMEORIGIN` while the host sent `DENY`, and browsers may drop framing protection entirely on conflicting XFO; container aligned to `DENY` (`431e211`, matches helmet's `frameguard: 'deny'`). Live-verified in prod: L-17 object `interestTags` → **400** (was 500), H-3 gate → 404 on unknown UUID / 400 on malformed id, `/api/monitoring/health/full` → **401**, login→search→daily→profile→notifications→plans→stats→success-stories all 200, biodata `%PDF-`, logout→401, legacy `tricityshadi.com/api` still 200 for shipped mobile builds. H-1 and M-3 returned 403 but via `PREMIUM_REQUIRED` (the QA account is free, so the premium gate fires before the new channel-ownership check) — those two were proven in dev, not prod. Pre-migration DB backup taken (`/var/backups/tricitymatch/db-2026-08-20.sql.gz`). New strong DB password handed over in-session (never committed); `.env.production.example` documents the slot.

- 2026-08-20 **Launch pricing + admin-editable offer layer + support plumbing (MERGED `c8ced2d`, PUSHED, DEPLOYED to prod).** Prod was serving a national-player ladder (VIP ₹5,999/yr) into a **12-user / 0-active-subscription** market with the founding window never switched on — measured, not assumed. Full rationale + what-to-watch: **`docs/PRICING_LAUNCH_2026-08-20.md`**. **(1) Launch offer** — new `utils/launchOffer.js` + migration **000053** `AppSettings` (key/JSONB). The overlay lives INSIDE `utils/razorpay.js`, so `getPlanDetails` is the single resolved read behind createOrder, verify-payment, the webhook, the Google Play rail and admin grants — charged price, displayed price and the tenure written onto the Subscription row cannot drift. Fails **closed to regular pricing** on every failure path (no row / malformed override / passed deadline / disabled); a garbage `contactUnlocks` never widens a finite cap. Sync reads off an in-process cache (getPlanDetails runs inside payment transactions) warmed at boot, rewritten on save, 60s revalidate. Ladder: every tenure SHORTER than regular and every price far lower, per-month falling with commitment (299/275/237/217), **top tier ₹1,299 for 180 days** — 6 months not 12, so it can be re-priced. Basic alone keeps 30 days because the founding grant is 30 days and a paid tier shorter than the free grant is worse than free. MRP anchors are the regular price **prorated to the launch tenure**, not the headline year. **(2) Bundle inversion caught + fixed** — the top-ups are documented as priced above every plan's per-unlock rate; at launch prices ₹1,499 for 10 unlocks sat *at the VIP plan price*. Re-based to ₹199/3 + ₹549/10 (both above the ₹49.8 floor), and **bundle_25 withdrawn** — every price clearing the floor lands at/above VIP. Withdrawn means `getBundleDetails` returns null so `createBundleOrder` **refuses it**, not merely un-rendered. **(3) Founding window opened** per owner decision: 30 days, **3 unlocks** (FOUNDING_CONTACT_UNLOCKS 5→3), cap 500; endDate is now **signup + grantDays clamped to the window deadline** (was the shared cohort date, so a last-day signup out-lived the offer). Verified live: a fresh signup got `founding_premium`, 3 unlocks, 2026-08-20→09-19, amount 0. **(4) Admin editor** `/admin/launch-offer` — prices/tenures/unlock caps/MRP/deadline/banner copy/bundle withdrawal/founding window, in ₹ and DAYS (a pricing screen that asks a human for paise is a mis-charge waiting to happen), each row showing what it reverts to. Validation lives in `saveOffer` (one place): rejects zero/negative/absurd amounts, tenures outside 1–730, an MRP below the offer price, a non-ISO deadline, and an **explicit null founding unlocks** (null = unlimited downstream = free phone-number harvest). Live round-trip proven: edited VIP 1299→1399 in the UI → `/subscription/plans` served 1399 → reverted. **(5) 🔴 Copy drift found by screenshot** — hardcoded bullets survived the re-term: cards said "5 contact unlocks" beside a 6-unlock plan, "6-month validity" on a 4-month tier, "Full-year validity" on a 6-month VIP, and the compare table's unlock row was frozen at 0/5/10/25. Backend FEATURE_COPY is now built from the effective plan and web `planFeatures()` re-terms the unlock/validity lines from the live plan; the compare row is filled from the API; mobile's `getPlans` overlay was only overlaying **price** (so RN showed launch prices against regular tenures) and now takes tenure/unlocks/MRP/badge too, plus a live `getUnlockBundles`. **(6) Anti-harvest** — unlimited tiers had NO daily unlock ceiling, so one cheap VIP was a scriptable export of the directory; added a rolling-24h cap (`UNLIMITED_DAILY_UNLOCK_CAP`, default 25). **(7) Support plumbing** (owner chose "fix the plumbing") — support was **write-only**: the contact form stored the enquiry and emailed SUPPORT_EMAIL, admins could only flip new/read/resolved, and there was no answer path. Added migration **000054** + `POST /admin/contact-messages/:id/reply` + a branded `supportReply` template (human text **escaped** — the only human-typed HTML interpolation in email.js) + a reply box in the Support Inbox; a failed send is surfaced and records nothing (an admin who believes they replied and didn't is the failure this exists to prevent). Verified end-to-end: enquiry → reply → real email → row resolved. Also: **placeholder WhatsApp `919876543210`** shipped as a live tappable link in the web footer AND RN Support (plus a placeholder footer phone) → both config-gated (`VITE_SUPPORT_*` / `EXPO_PUBLIC_SUPPORT_*`), **hidden when unset rather than fake**; new **`/help` Help Centre** (FAQ answering what the inbox actually receives + channels, sitemap'd, linked from footer + Settings → More) since members had no in-product route to support at all. **Gates: BE 356 unit (33 suites) · FE 112 · mobile tsc 0 · eslint 0 errors · FE build ✓** (slop-lint still red on the same 4 pre-existing hexes in untouched files). Screenshots in repo root: `subscription-launch-pricing-web.png`, `admin-launch-offer-editor.png`, `admin-launch-offer-founding.png`, `support-inbox-reply.png`, `prod-subscription-launch-pricing.png`. **DEPLOYED same day** (pre-migration backup `/var/backups/tricitymatch/db-pre-launch-pricing-2026-08-20.sql.gz`; migrations 000053+000054 applied on boot via umzug; backend then frontend `--force-recreate`, co-tenants verified 200 throughout). Prod live-verified: launch ladder + `launchOffer.active` + founding `{open:true, 3 unlocks, 30 grantDays}` on `/subscription/plans`; bundles served as 199/549 with bundle_25 absent; `/help` 200 + sitemap'd; `/admin/launch-offer` **401 unauthenticated** and renders the real ladder when signed in; a real prod signup minted `founding_premium` / 3 unlocks / 2026-08-20→09-19 / ₹0 and the test account was deleted after. **Prod DOES have live Razorpay keys** (`rzp_live`) — this pricing takes real money now. Follow-up `2377a32`: the "Most Popular"/"Best Value" ribbon wrapped to two lines in the five-column grid and overlapped the plan title — caught on the live page, fixed and redeployed. **Method note: prod CORS rejects no-Origin WRITES; API smoke of any POST/DELETE needs `X-App-Client: mobile`** (the native-client escape hatch), otherwise every write 403s "Not allowed by CORS". **Owner items: set the real offer deadline in Admin → Pricing & Offers · live Razorpay keys (nothing can take money without them) · Play Console product prices do NOT follow this ladder (set them there; product IDs are permanent) · point SUPPORT_EMAIL at a mailbox somebody reads · supply the real WhatsApp/phone or leave them hidden · name the DPDP Grievance Officer.**

- 2026-08-22 **Single-plan launch pricing + admin card show/hide (commit `da90ceb`, PUSHED + DEPLOYED to prod 2026-08-23).** Owner call: sell ONE thing while the corpus is small. **(1) Ladder collapsed to one card** — `DEFAULT_PLAN_OFFERS` now puts `premium_plus` ("Premium") on sale at **₹1,199 / 90 days / UNLIMITED contact unlocks**, MRP anchor ₹2,499 (the regular 90-day price, so the strike-through compares like with like — 52% off), and withdraws basic/elite/vip/nri. `premium_plus` is the survivor because its enum key already reads "Premium", it is natively the 90-day rung, and it carries the profile-boost + spotlight entitlements the copy promises. `requireVIP` exists but gates **no route**, so nothing became unbuyable. Live-proven: `create-order` → **400 "That plan is not available right now"** for vip/nri/elite, **200** for premium_plus. **(2) Defaults alone were not enough** — `initLaunchOffer` seeds `DEFAULT_PLAN_OFFERS` only on a boot with NO `launch_offer` row, and dev+prod both carry one from the 2026-08-20 deploy, so changing the constant would have shipped a five-tier page while the code claimed one. Migration **000057** rewrites the persisted row's `plans` (everything else — deadline, headline, bundles, founding window — carried across), snapshotting the old ladder to AppSettings key `launch_offer_plans_pre_single` so `down` genuinely restores it. **(3) Card show/hide is now a real admin lever** — the per-tier "On sale" toggles already existed; what was missing was the ceiling and the floor. `MAX_VISIBLE_PAID_PLANS = 5` (the five paid tiers, NRI included; Free always renders and is not a launch-offer tier → **6 cards max**), and `saveOffer` now REFUSES a save with every tier withdrawn — a pricing page with nothing on it takes no money and, unlike a bad price, nothing downstream fails loudly. Editor gained a live **"N of 6 cards showing"** badge, an inline warning, and a client-side block before the round-trip. Verified live both ways: NRI on → `/subscription/plans` serves 2 paid; all-hidden → **400** from the API and blocked in the UI. **(4) 🔴 Real bug the toggles would have hit** — `Subscription.jsx` decided "did the API serve plans?" by sniffing `plans.basic_premium || plans.premium_plus || plans.vip`. Withdraw exactly those three and the sniff reads false, the static `PLAN_CONFIG` fallback fires, and the page **resurrects every card the server just took off sale, at hardcoded prices checkout refuses**. Replaced with a `plansLoaded` flag set on a successful response. **(5) Copy the single card made visibly wrong** — the compare table said Premium has **no** profile boost while the card directly above it said it does (backend `PLANS.premium_plus.features` lists `profile_boost` and `searchController.premiumBoost()` gives it **+10**, so the table was simply false); column header read "Plus" against a card headed "Premium"; and a row no visible plan offers ("Relationship manager") rendered as a line of dashes advertising something nobody can buy — rows with no visible ✓ are now dropped. Header re-words to "Go Premium" when one paid card shows. Premium's bullet list gained a validity line ("3 months of full access") on both the server copy and web `planFeatures` (the latter via a `/validity$/` placeholder that `retermForLivePlan` rewrites from the live plan). **Mobile needed no change** — `api/subscription.ts` already drops tiers the server omits and overlays every commercial term. Gates: **BE 474 unit (+4) · FE 118 + build ✓ · mobile 57 + tsc 0 · eslint 0 errors · slop-lint clean**. **DEPLOYED 2026-08-23** (pre-migration backup `/var/backups/tricitymatch/db-pre-single-plan-2026-08-22.sql.gz`; migration 000057 applied on boot via umzug — prod's PREVIOUS ladder, which had been admin-edited away from the code defaults [basic ₹299/30d/6], is snapshotted in AppSettings key `launch_offer_plans_pre_single` and is what `down` restores). Prod live-verified: `/subscription/plans` serves Free + Premium ₹1,199/3 months/unlimited only; bundles 199/549; founding still open 3 unlocks/30d; `create-order` **400s vip/nri/elite/basic_premium**; `/admin/launch-offer` 401 unauthenticated; health OK; legacy `tricityshadi.com/api` 200 for shipped mobile builds; co-tenants (edumapping, school.globoniks, tricitylifeinsurance) 200 throughout. **Pre-existing co-tenant issue noticed, NOT touched:** `college-placements.globoniks.com` serves a 301 over HTTP but its **TLS certificate has expired** — unrelated to this deploy (nginx was never reloaded, only `tricitymatch-*` containers recreated); owner's call. **Owner items: set the real offer deadline in Admin → Pricing & Offers (currently 18 Nov 2026, auto-seeded) · Play Console product prices do NOT follow this ladder (set them there; product IDs are permanent).**

- 2026-08-25 **Admin repair + sub-admins + growth/measurement pass (commits `4e99d50`, `97971b7`, PUSHED + DEPLOYED to prod).** Two tracks: the reported admin bugs, then P0/P1 of a production priority audit measured live off the prod DB (15 users, 6/15 profiles with a photo, 13 match actions, **4 mutual matches and 0 messages ever**, 4 abandoned checkouts, ₹0 real revenue — the three "active subscribers" were 2 founding grants + 1 hand-granted plan). Report artifact: "TricityMatch Liquidity Gap". **Admin bugs (all reproduced first):** 🔴 `AdminUserDetail` PLAN_OPTIONS was still `['free','basic','premium','gold']` — not enum values — so **every plan override 400'd** at `body('planType').isIn(ALL_PLANS)` while the panel said only "Update failed"; options now come from a new `GET /admin/plan-options` resolved against the live launch offer (withdrawn tiers returned but flagged, founding only while open). 🔴 `AdminUsers` read `u.Subscription` (singular) against a hasMany serialized as `Subscriptions` → the plan column said **"Free" for every member**, including one just upgraded; both admin surfaces now read a server-derived `activePlan`/`activeSubscription` computed with the SAME predicate as `utils/entitlements` (the newest row is routinely a `pending` order nobody paid). 🔴 admin grants ran a flat **30 days regardless of plan** (90-day Premium → 30 days); term now follows `planDetails.duration`. 🔴 member invoice button gated on `s.paymentId` but the API sends `razorpayPaymentId` → **no member could ever download an invoice**, and "Total spent" read the non-existent `paymentAmount` → ₹0 for everyone. 🔴 both invoice endpoints issued receipts for **orders created and never paid** (amount>0 was the only check) → now refuse unpaid + ₹0-grant rows, buttons hide instead of always failing (blob error bodies parsed so the real reason shows). 🟠 admin detail read `user.verificationStatus` — no such column — so the Verified chip never rendered. Admin sign-out went to the `/admin/login` stub. **Sub-admins (migration 000059):** `sub_admin` role + `Users.adminPermissions` JSONB, 10 scopes, `admin`/`super_admin` hold all implicitly (`constants/adminScopes.js`, fails closed on a malformed column); every admin route carries `requireAdminScope()` — verified live that a support-only sub-admin gets 200 on support/reports and **403 on users/revenue/pricing/team**, including a hand-crafted plan grant. `/admin/team`: create an admin, **promote an existing personal account**, edit scopes, revoke. Guard rails: no self-edit, no granting above your own rank, no granting a scope you do not hold, cannot demote the last full admin. Scoped accounts land on the first section they can open; a page they lack shows "Not your section". **Method note: `auth` loads the user with an attribute allowlist — `adminPermissions` had to be added or every sub-admin resolved to zero scopes and 403'd on its own pages.** **Measurement:** `trackEvent` already existed and was wired (12 rows in prod) — what was missing was the **pre-signup half** and any way to read it. Added 4 traffic stages via a public beacon `POST /api/v1/events` (rate-limited; a separate CLIENT_EVENT_TYPES allowlist so a curl loop cannot forge `account_created`), an admin **Funnel** page (window + previous window; step % suppressed when >100 since stages are not strictly nested) and an **Audit Log** page — `logAudit` now persists to `AuditLogs` (migration 000060) alongside the JSON log. **Lifecycle mail (none of it existed):** abandoned-checkout / renewal T-7 / expiry-day / win-back + a no-photo nudge, each at most once per row via `lifecycleMail` JSONB, marked only when delivery actually succeeded, capped per run. Weekly digest counted only *newly joined* profiles so it reached **3 of 13**; now falls back to profiles the member has not seen. **🔴 Incident I caused, then fixed:** the dev `.env` carries the **same Resend key as production**, and a first uncapped run of the photo-nudge job fanned out over 5,000 seeded loadtest accounts and **exhausted the shared daily quota** ("You have reached your daily email sending quota"). Prod sent nothing that day after 06:19 UTC so no member mail is known to have failed, but production OTP/reset was at risk until the UTC reset. Fixes: **`EMAIL_DRY_RUN` defaults ON outside production** (renders + logs, returns success so callers exercise real paths) and every lifecycle query is `limit`-capped. Owner item: give dev its own Resend key or strip it from `.env.development`. **Growth:** community × city landing pages (5 communities × 3 cities, `/matrimony/:city/:community`, sitemapped — each composes a distinct community block + the city's own locality block, same honesty bar as the city pages); **refund & conduct policy** page + footer/Help links; photoless profiles sort last in search (−40, below any boost); prioritised profile nudge on the dashboard (photo only there — the dashboard already has its own verification card); admin **cancel plan**, **member CSV export**, editable **lead status**, unread-support + no-photo badges, **founding-window monitor** (2/500, closes 18 Nov). NRI FAQ added to Help + Home; stale "Premium vs VIP" answer replaced. **🔴 Deploy bug found + fixed (`97971b7`): compose passes ONLY the vars in its explicit `environment:` map, so `FREE_REPLY_WINDOW=true` in the prod `.env` did nothing** — the same failure the comment directly above that block warns about (CORS_ORIGIN), made a second time; flag + `ASTROLOGER_MARKETPLACE` + `UNLIMITED_DAILY_UNLOCK_CAP` + `EMAIL_DRY_RUN` added to the allowlist. **FREE_REPLY_WINDOW is now LIVE in prod** (free members read forever + 5 sends/48h after a mutual match) — flipped because 4 mutual matches had produced zero messages, the paywall sitting exactly on the magic moment. Gates: **BE 499 unit · FE 118 · lint 0 errors · mobile tsc 0 · slop-lint clean · build ✓**. Deployed with pre-migration backup `/var/backups/tricitymatch/db-pre-admin-growth-2026-08-25.sql.gz`; migrations 000058/000059/000060 applied on boot (000058 correctly left the hand-edited ₹1,100 price alone). Prod verified: health, `/refund-policy` + all 3 community pages 200, sitemap 30 URLs, beacon 204, admin funnel/audit/team 200, `freeReplyWindow:true`, `email.dryRun:false`, legacy `tricityshadi.com/api` 200, co-tenants 200 throughout. **Owner items: prod price still reads ₹1,100 (someone saved that after the ₹1,099 change) · Sentry DSN, Google client ID and a real WhatsApp number are all still unset · supply is the bottleneck (1–2 signups/week) and no code fixes it.**

- 2026-08-29 **B2B marketing channel: proven end-to-end on PROD, then built out (commits `8ee0f09`→`fe163fe`/`281a1f3`, PUSHED + DEPLOYED).** Task was "create a marketing user on prod, approve it, and prove admin → marketing → member → paid subscription reflects on both dashboards". **🔴 The funnel had never worked.** `authController` signup ran `sequelize.literal('usageCount + 1')`; Postgres folds an unquoted identifier to lower case, so it resolved to a non-existent `usagecount` and threw INSIDE the signup transaction — every signup carrying a valid referral code rolled back with "Unable to create account. Please try again." Not one referred signup could ever have been created in production. (`profileController:896` already quoted its counterpart, which is what the fix follows.) **Live chain then driven on prod:** admin creates `priya.field@tricitymatch.com` → status lever proven (set `inactive` → login 403s "Account is not active" → set `active`) → rep logs in, mints `PRIYAF47YDTW` for "Sector-17 Roadshow" → member signs up through `?ref=` → lead lands on both dashboards → **real Razorpay payment** → subscription active, lead `converted`/`paid`, both dashboards + admin revenue move. **Payment method (owner-approved):** prod runs LIVE keys, so a genuine charge was not acceptable; prod was swapped to the dev **Razorpay TEST keys** for a ~10-minute window (`.env` backed up, backend recreated), the checkout driven for real in a browser against a **locally served Razorpay Checkout page** (no prod frontend rebuild needed — the page carries the test key and the prod-created order id), test card → mocksharp "Success" → real `pay_…` + signature → prod `verify-payment`. Live keys restored and `diff`-verified identical, backup deleted. `verifyPayment` is **HMAC-only** — it never fetches the payment to confirm capture; safe because only Razorpay and the server hold the secret, but worth knowing. **🔴 Second bug found by the test:** admin revenue read **₹2,200 / 2 transactions** when exactly one payment existed — an **admin plan grant writes the plan's list price with no payment reference**, and all four revenue reads summed on `amount` alone, so every comped plan was counted as money taken. Now every revenue read requires `razorpayPaymentId IS NOT NULL` (that column also stores the Google Play purchase token, so store purchases still count). **Then built on request:** per-rep **member report** — one row per invited member with signed-up / paid / plan / amount, over a funnel summary — from a single builder (`utils/marketingReport.js`) behind BOTH `GET /api/marketing/report` and `GET /api/v1/admin/marketing-users/:id/report`, so a rep and an admin can never see different stories; "paid" reads the Subscription row, not the lead's denormalised flag. Both views **auto-refresh** (20s poll + refetch on focus, paused while hidden — `useAutoRefresh`). **Admin-set commission** (`utils/marketingCommission.js`, AppSettings key `marketing_commission`, default **20%**, shape already allows per-rep overrides): editor on Admin → Marketing Users, and the rep sees the rate, the earned total and a per-member commission column so they can add it up themselves; every failure path falls back to the default rather than reporting ₹0. **🔴 Dark mode (user-reported screenshot):** the marketing portal had **zero `dark:` classes** — white panels under the dark theme left rows of near-invisible text; and the portal renders no member Navbar, which was the only thing mounting `useDarkMode`, so a hard load came up light regardless. Both fixed, plus a toggle in the rail. Also: rail links across to the member site (Browse Profiles / Open the Website) and a **staff-portal switch in the member navbar** (marketing → portal, admin → panel); `$` icon replaced with ₹ on rupee tiles and `en-IN` digit grouping. **Prod live-verified:** rep report and admin report agree (1 invited / 1 signed up / 1 paid / ₹1,100 / ₹220 commission at 20%), revenue reads 1 transaction, commission PUT rejects 150. Gates: **BE 507 unit (50 suites) · FE 118 · builds ✓ · lint 0 errors**. **Method notes: GSAP `tl.set()` is a zero-duration tween and renders on CREATION** — `immediateRender:false` does not save you under the hyperframes renderer either; six scene "hard kills" silently blanked every scene but the last, and the fix was to end the fade just before the clip boundary instead. A running `hyperframes preview` **rewrites your `index.html`** (stamps `data-hf-id`). Brag video of the whole chain: `brag-output/brag.mp4` (24.1s). **Payout ledger added same day (migration 000061, `MarketingPayouts`):** commission earned is derived from the current rate, so it moves when the rate moves — which is why it cannot also be the record of what was handed over. Payouts are real rows with the rate snapshotted onto each, an admin records/queues/settles/removes them from the rep detail page, and the rep's dashboard shows earned / paid out / queued / outstanding over the individual payments. Queued payouts count against the balance (or an admin queues a transfer, still sees it outstanding, and queues it twice); outstanding is floored at 0 with any surplus reported as `overpaid` (a rate cut can leave earned below what was already paid, and a negative outstanding reads to a rep as a debt they owe); recording above the balance is refused unless `allowOverpay` is sent. Prod-verified: overpay guard fires, ₹200 paid + ₹75 queued → rep sees both, "Mark paid" settles to ₹275/₹0 outstanding, and a rep POSTing to the admin payout route gets 403. **Test data left on prod** (`priya.field@`, code `PRIYAF47YDTW`, member `rohit.roadshow.qa@`, one ₹1,100 test-mode subscription) — it is what the dashboards are showing; delete when the demo is done.
- 2026-08-29 **Admin panel dark mode (commit `53a5c21`, PUSHED + frontend DEPLOYED).** Same bug class as the marketing portal: `/admin/*` mounts no member Navbar, so nothing applied the saved theme on a hard load; and once `html.dark` WAS set, the global `html.dark .bg-gray-900` inversion in `index.css` painted the sidebar near-white under white text. `AdminLayout` now mounts `useDarkMode` + a rail toggle; sidebar/topbar use `admin-chrome` (dark in both themes); `.admin-panel`-scoped dark rules cover status chips (green/red/amber/blue/primary tints + text), hover rows, dividers; Recharts grids use `stroke="currentColor"` under `.chart-grid`. Verified live dev (dashboard/users/pricing/support) + prod (class on hard load, rail rgb(11,14,22)). **Marketing first-sign-in "server error" (user report) — NOT reproduced** in 3 sequences (dev fresh rep, prod fresh rep, prod member→UI sign-out→rep): every first login landed on the dashboard. Prod nginx shows the pattern as two `POST /api/auth/login` **both 200**, 2s apart, with ZERO client requests between — the server never failed; the client threw after a 200 (likely the `ErrorBoundary` "Oops! Something went wrong" card, whose Try Again returns to the form). Need the exact on-screen text/screenshot to go further. Side observation: the 4 Cloudinary profile-photo **503s seen in this Playwright run were a FALSE ALARM** — the response was synthesized by the Playwright MCP sandbox (2 ms, `content-type: text/plain`, no `server: cloudflare`/`cf-ray`, body literally `Offline`): the sandbox blocks third-party hosts, so `res.cloudinary.com` never received the request. curl (any UA/Referer) → 200; account 0.04% quota; no incident. **Method note: a 503/blocked third-party asset inside the Playwright MCP browser is the sandbox allowlist, not the CDN — check `server`/`cf-ray` headers and the body before reporting.** Still shipped `components/ui/RetryImage.jsx` (`a247d6c`, deployed) as a genuine hardening: drop-in `<img>` retrying once after 1.5s with `?r=1` before the caller's fallback, at 12 member sites — a real transient CDN error previously hid the photo for the whole session. FE 121 tests.

## Project Status (2026-06-16)
**✅ Complete:** audit (9 phases) + remediation (3 Highs + ~all M/L) · dependency security (0 runtime vulns) · DB migrations through **000039** (applied) · SEO (robots/sitemap/per-route meta) · regression tests · build green · **build-work pass: 9 stale unit tests fixed (BE 116/116 unit green) · kundli PDF · SEC-4 full magic-byte · family-group chat backend (built + membership-gated + mobile client wired) · web i18n (Navbar+Login ×3 langs) · web in-browser calls (Agora)** · FE 31/31 + FE build green.
**🔧 Incomplete (needs creds/config, not code):** Razorpay/Email/Google-OAuth/SMS/FCM live keys · `VITE_AGORA_APP_ID`+Agora server creds (web/mobile calls) · Redis (dev).
**🔧 Incomplete (deliberately deferred):** held dep migrations (react 19 [mobile-blocked], tailwind 4, express 5, multer 2, umzug 3, eslint 10) · remaining web pages i18n (Home/About/Contact/etc. still English) · backend integration+e2e need a live DB to run (auth.test.js).

## Audit System
Full-project audit runs chunked, never one pass. **Tracker = single source of truth: `review-progress.md` (repo root).** Read before any review chunk, update after. Methodology + phase→skill map: `docs/09_Audit_System.md`. Rules: evidence (`file:line`) for every finding · never assume/hallucinate code · severity Critical/High/Medium/Low · report-only skill first (`/qa-only`,`/code-review`), fix in separate chunk. 9 phases (Architecture→Backend→DB→Security[OWASP]→Frontend→SEO→Performance→QA→Missing Features); skills: `/health`,`/code-review`,`/security-review`,`/cso`,`/design-review`,`/browse`,`/benchmark`,`/qa-only`→`/qa`,`/spec`. Resume after context loss: open tracker → first non-✅ phase → first `[ ]` item. **Audit COMPLETE (2026-06-16)** — all 9 phases ✅, 53 findings remediated (3 Highs + ~all M/L); see tracker Remediation Log + Project Status above. Prior seeded gaps resolved: robots.txt/sitemap added, route-guard component tests added.

## gstack Skills (REQUIRED)
```bash
test -d ~/.claude/skills/gstack/bin && echo GSTACK_OK || echo GSTACK_MISSING
# MISSING: git clone --depth 1 https://github.com/garrytan/gstack.git ~/.claude/skills/gstack && cd ~/.claude/skills/gstack && ./setup --team
```
Always use matching skill. `/browse` all web (never WebFetch/Search) · `/qa` after feature · `/design-review` after UI · `/review` before commit · `/investigate` unknown bugs · `/ship` deploy · `/run` start/verify · `/verify` fix works · `/code-review` (ultra=cloud) · `/simplify` reuse · `/ui-ux-pro-max` UI design · `/spec` before complex feature · `/security-review` before auth/payment/data code.

## magic-mcp (UI component MCP) — usage doctrine
`mcp__magic__21st_magic_component_inspiration` (returns snippets/ideas, no file) + `_builder` (writes a component). **WEB ONLY** — it emits React + Tailwind + shadcn/Radix idioms; **useless for `mobile/` RN** (no RN output). Use it for **inspiration**, then **recreate in the project's own brand tokens** (burgundy/gold, existing `FormField`/`Card`/`SectionHeader`, `index.css` vars) — its raw output uses `bg-background`/`text-foreground`/`hsl(var(--…))` shadcn tokens + npm deps that do NOT match this codebase. **Do NOT bolt magic-mcp components in wholesale** — the web UI is a cohesive 2026-06-23 redesign; a generic dropped-in card/section degrades it. Litmus: only adopt if it clearly beats the existing purpose-built component (e.g. the `OtpBoxes` segmented input was worth adapting; generic "profile cards" did NOT beat the matrimony `ProfileCard`). Verify the existing component first; reach for magic-mcp when there's a real gap.

## Skill routing

When the user's request matches an available skill, invoke it via the Skill tool. When in doubt, invoke the skill.

Key routing rules:
- Product ideas/brainstorming → invoke /office-hours
- Strategy/scope → invoke /plan-ceo-review
- Architecture → invoke /plan-eng-review
- Design system/plan review → invoke /design-consultation or /plan-design-review
- Full review pipeline → invoke /autoplan
- Bugs/errors → invoke /investigate
- QA/testing site behavior → invoke /qa or /qa-only
- Code review/diff check → invoke /review
- Visual polish → invoke /design-review
- Ship/deploy/PR → invoke /ship or /land-and-deploy
- Save progress → invoke /context-save
- Resume context → invoke /context-restore
- Author a backlog-ready spec/issue → invoke /spec

## QA method notes (web, Playwright)
- **Static full-page screenshots LIE about scroll-animated content.** `react-countup` (`enableScrollSpy`) renders at start value (**0**) and `framer-motion whileInView` stays `opacity:0` until in view → a `fullPage` shot shows "0+/0K+" stats and "blank" sections that are **artifacts, not bugs**. Scroll the element into view (`window.scrollTo`) THEN screenshot before flagging.
- Onboarding/Login render the **global Navbar** (sticky) — in `fullPage` shots it can overlap mid-page; viewport-level shots are truer.
- Dev OTP e2e: local `OTP_BYPASS_CODES` is **unset** by default (prod-only per memory) + SMS is live (real texts). To drive a full verify e2e, set `OTP_BYPASS_CODES=000000` in `.env.development`, **touch `backend/server.js`** to make nodemon reload (it does NOT auto-restart on `.env` change, and an external `kill` of its child does not respawn it), test, then revert. Don't send real SMS to arbitrary numbers.
