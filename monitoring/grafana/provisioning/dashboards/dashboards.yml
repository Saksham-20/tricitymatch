# Grafana Dashboard Provisioning

apiVersion: 1

providers:
  - name: 'TricityMatch Dashboards'
    orgId: 1
    folder: 'TricityMatch'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
