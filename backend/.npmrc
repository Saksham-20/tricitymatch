# multer-storage-cloudinary@4 peer-requires cloudinary@^1, but we run cloudinary@2
# (security fix for arg-injection). The combo is runtime-compatible for the
# upload_stream API we use; legacy-peer-deps lets npm install resolve it.
legacy-peer-deps=true
